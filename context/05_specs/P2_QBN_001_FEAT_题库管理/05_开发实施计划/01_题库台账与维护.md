# 题库台账与维护 实现计划

> **目标：** 落地 questions 表与题目全生命周期维护链路（列表分 tab 查询、详情、编辑、启停、删除），前端题库管理页列表态与查看/编辑弹窗可用。
> **架构：** 后端按 account/dimension 样板四层贯通（domain → repository → service → handler → router → wire），题目编辑/启停/删除走 version 乐观锁（dimension 域 UpdateWithVersion 同款）；前端新建 question-bank feature 三件套 + 双 tab 列表页挂 _authenticated，顶栏菜单「评估运营」组内接通题库入口。批次/量表/生成三域不在本子计划，question_batches 相关字段（batch_id）本计划只落列不消费。
> **技术栈：** Go 1.25 + Gin + GORM + Wire；React 19 + TanStack Query + shadcn/ui + i18next

---

## 代码库依据

- 相关入口与复用：
  - 分层样板：[hr-backend/internal/domain/dimension.go](../../../../../hr-backend/internal/domain/dimension.go)、[repository/dimension.go](../../../../../hr-backend/internal/repository/dimension.go)、[service/dimension.go](../../../../../hr-backend/internal/service/dimension.go)、[handler/dimension.go](../../../../../hr-backend/internal/api/handler/dimension.go) 四层同构，question 域照此新建。
  - 乐观锁先例：repository `UpdateWithVersion(ctx, id, version, updates) (int64, error)`（WHERE id+version+deleted_at IS NULL，version=gorm.Expr 自增），service 把 RowsAffected==0 映射 VersionConflict 错误码（dimension 1204 同款）。
  - 错误模型：`service.Error{Code, Msg}` + `service.NewError(errcode.X)`（[service/account.go:62-74](../../../../../hr-backend/internal/service/account.go)）；handler 统一 `handleServiceError`（仅 1003 映 401，其余 HTTP 200 带 code）。17xx 段 errcode 全空可用。
  - 关键词 LIKE：`likeescape.EscapeLike`（internal/pkg/likeescape）+ `ESCAPE '\'` 子句，account ListAccounts 先例。
  - 迁移：[model/migrate.go](../../../../../hr-backend/internal/model/migrate.go) `allModels()` 登记新模型；雪花 ID 由 model 包全局 Create 回调透明赋值（新模型零配置）。
  - 前端：feature 三件套样板 [features/account/](../../../../../hr-frontend/src/features/account/)；httpClient 已解包 `{code,message,data}`；分页类型 `Page<T>` 在 [lib/contracts.ts](../../../../../hr-frontend/src/lib/contracts.ts)；查询区+表格+分页+行操作最完整先例 [features/assessment/components/batch-table.tsx](../../../../../hr-frontend/src/features/assessment/components/batch-table.tsx)；tab 手写先例 [routes/_authenticated/assessment/index.tsx](../../../../../hr-frontend/src/routes/_authenticated/assessment/index.tsx)（无 Tabs UI 组件，手写 role=tablist button）；确认弹窗通用封装 [components/confirm-dialog.tsx](../../../../../hr-frontend/src/components/confirm-dialog.tsx)；nav-menu.tsx:52 已有 F5 题库占位，接 to 即激活。
  - 维度名回显：复用 GET /api/dimensions/tree（DimensionBrief 含 name/enabled），SCALE tab 的型别维度在 SP3 引入前为空属预期。
- 验证入口：后端 `cd hr-backend && go build ./... && go test ./...`；前端 `cd hr-frontend && pnpm type-check && pnpm test`。基线实测（2026-09-23）：后端 build 通过、前端 type-check 通过。
- 迁移方式：GORM AutoMigrate（migrateDB B 段），新模型在 allModels() 登记，无独立迁移文件。04 文档中 staging 列的 MySQL MEDIUMTEXT 钩子归 SP4。
- 规模与耦合判断：题目域是题库管理的地基（SP2 批次审核、SP3 量表引入、SP4 生成都写 questions 表），本计划先落表结构与行级维护闭环，独立验收（列表/详情/编辑/启停/删除全链路）。共享文件 router.go / wire.go / contracts.ts / nav-menu.tsx 由后续子计划顺序扩展，各自保持可编译可运行，无 prefactor 需求。

---

## 文件结构

### 新建文件
- `hr-backend/internal/domain/question.go` — Question 模型（04 §3.1 GORM struct 权威）与 source/status/scale_key 枚举常量
- `hr-backend/internal/repository/question.go` — QuestionRepository 接口与实现（分页查询/详情/乐观锁更新/启停/软删/编号分配）
- `hr-backend/internal/service/question.go` — QuestionService 接口与实现（列表组装/详情 DTO/编辑校验/启停/删除校验）
- `hr-backend/internal/api/handler/question.go` — QuestionHandler（questions 五接口）
- `hr-backend/internal/repository/question_test.go` — repository 黑盒测试（:memory: SQLite，仿 dimension_test.go）
- `hr-backend/internal/service/question_test.go` — service 测试（fake repo，仿 dimension_test.go dimFakeRepo）
- `hr-backend/internal/api/handler/question_test.go` — handler 测试（fake service）
- `hr-frontend/src/features/question-bank/api.ts` — questions 五接口纯函数请求
- `hr-frontend/src/features/question-bank/hooks.ts` — TanStack Query 封装（列表/详情/编辑/启停/删除）
- `hr-frontend/src/features/question-bank/types.ts` — 域私有类型（tab 键、查询条件、表单值）
- `hr-frontend/src/features/question-bank/components/question-table.tsx` — 单 tab 列表（查询区+表格+分页+行操作，双 tab 各持一份）
- `hr-frontend/src/features/question-bank/components/question-view-dialog.tsx` — 题目查看弹窗（含编辑入口）
- `hr-frontend/src/features/question-bank/components/question-form-dialog.tsx` — 题目编辑弹窗（RHF+Zod）
- `hr-frontend/src/features/question-bank/components/__tests__/` — 上述组件测试
- `hr-frontend/src/routes/_authenticated/question-bank/index.tsx` — 题库管理页（双 tab + 页面级按钮 + 弹窗编排）

### 修改文件
- `hr-backend/internal/pkg/errcode/errcode.go` — 新增 17xx 段常量与 messages（本计划用 1701/1705/1707/1708/1713，全量 1701-1709+1713 一次性定义）
- `hr-backend/internal/model/migrate.go` — allModels() 登记 &domain.Question{}
- `hr-backend/internal/api/router/router.go` — NewRouter 形参加 QuestionHandler，auth 组挂 questions 五路由
- `hr-backend/wire.go` — wire.Build 追加三个 New*（再跑 go generate）
- `hr-frontend/src/lib/contracts.ts` — QuestionListItem/QuestionDetail 等 questionbank 契约类型 + ErrCode 17xx
- `hr-frontend/src/components/nav-menu.tsx` — F5 题库占位接 to: '/question-bank'（挂评估运营组，specs 4.1.1）
- `hr-frontend/src/i18n/locales/zh.json` / `en.json` — 新增 questionBank 命名空间（zh/en 同步）

---

## 数据模型来源

| 来源 | 文件路径 | 状态 |
|------|----------|------|
| 04_model_interface.md | context/05_specs/P2_QBN_001_FEAT_题库管理/04_model_interface.md §3.1 | questions 表本次落地；§3.2/§3.3（batches/generations）归 SP2/SP4 |

---

## 任务清单

- [x] **T1: questions 表迁移（数据库任务）**

  **文件：**
  - 引用：`/context/05_specs/P2_QBN_001_FEAT_题库管理/04_model_interface.md` → questions 表 CREATE
  - 创建：`hr-backend/internal/domain/question.go`
  - 修改：`hr-backend/internal/model/migrate.go`（allModels 追加 &domain.Question{}）

  **步骤：**
  1. 按 04 §3.1 的 GORM 模型 struct 原样落地 `Question`（含全部 gorm/json tag，ID/dimension_id/batch_id json 带 `,string`），同文件定义枚举常量：`QuestionSourceAI = "AI"`、`QuestionSourceScale = "SCALE"`、`QuestionStatusPending/Active/Disabled/Rejected`、`ScaleKeyRisoHudson = "RISO_HUDSON"`、`ScaleKeyEssence = "ESSENCE"`。
  2. migrateDB 不加新钩子（首启建表由 AutoMigrate 承担），allModels() 登记即可。
  3. 验证：`cd hr-backend && go run ./cmd/server`（需 Redis）首启建表后，用 sqlite3 或 GORM 脚本确认 `questions` 表存在且列集与 04 §3.1 字段说明一致（question_no/scenario/requirement/focus_point/status/version/deleted_at 等 16 列 + uk_questions_question_no 唯一索引）。开发环境 SQLite 路径 data/sili-smart-hr.db。
  4. 提交（控制器统一执行）。

  **验收锚点：**
  - 验证命令：`go run ./cmd/server` 启动无迁移报错后停进程；`sqlite3 data/sili-smart-hr.db ".schema questions"`
  - 预期输出：含 `CREATE TABLE questions`、`question_no TEXT NOT NULL`、`UNIQUE INDEX uk_questions_question_no`、`deleted_at` 列的 DDL

  **specs 依据：**
  - 章节范围：§4.1.2 B（显示字段）
  - 业务规则索引：
    - [BR1] §4.1.2 B 题目编号：Q-AG/Q-Scale-四位流水，超四位扩展，编号唯一

  **依赖：** 无
  **提交信息：** feat(questionbank): questions 表模型与迁移登记

- [x] **T2: QuestionRepository 数据访问层**

  **文件：**
  - 创建：`hr-backend/internal/repository/question.go`
  - 测试：`hr-backend/internal/repository/question_test.go`

  **契约：**
  - 签名：
    ```go
    type QuestionRepository interface {
        // ListPage 分页查询列表 tab 主路径：source 必选；status 空=全部；恒排除 PENDING；
        // keyword 对 question_no 与 scenario 做 EscapeLike 转义后的 LIKE OR 匹配（ESCAPE '\'）；
        // 排序 updated_at DESC, id DESC。返回行集（不含长文本列截断逻辑，截断在 service）与 total。
        ListPage(ctx context.Context, source string, dimensionID int64, dimensionIDSet bool, status, keyword string, page, pageSize int) ([]domain.Question, int64, error)
        // FindByID 主键查，软删自动过滤。
        FindByID(ctx context.Context, id int64) (*domain.Question, error)
        // UpdateWithVersion 乐观锁更新：WHERE id=? AND version=? AND deleted_at IS NULL，version 自增。
        // 返回 RowsAffected（0=冲突或不存在）。
        UpdateWithVersion(ctx context.Context, id int64, version int, updates map[string]any) (int64, error)
        // SoftDeleteWithVersion 乐观锁软删：WHERE id=? AND version=?，GORM Delete 置 deleted_at。返回 RowsAffected。
        SoftDeleteWithVersion(ctx context.Context, id int64, version int) (int64, error)
        // ListByIDs 批量按 ID 查（SP2 确认入库的批内题目定位复用），软删自动过滤。
        ListByIDs(ctx context.Context, ids []int64) ([]domain.Question, error)
        // MaxQuestionSeq 按编号前缀查最大序号（含软删行 Unscoped，如 Q-AG- 前缀取 0007 的 7），无行返回 0。
        // SP3/SP4 编号分配复用。
        MaxQuestionSeq(ctx context.Context, prefix string) (int64, error)
    }
    ```
  - 关键逻辑：ListPage 用 `r.db.WithContext(ctx).Model(&domain.Question{})` 链式拼 WHERE；dimensionIDSet 用 bool 区分「未传」与「传空」（查询参数空串=全部），dimensionID 为 int64（handler 层 parseID 完成转换）；keyword 非空时 `Where("(question_no LIKE ? ESCAPE '\\' OR scenario LIKE ? ESCAPE '\\')", esc, esc)`；分页 Offset/Limit 钳制（page<1 归 1、pageSize 1~100 上限 100）。
  - 依赖：`sili-smart-hr/backend/internal/domain`、`internal/pkg/likeescape`（EscapeLike）、gorm。

  **验收锚点：**
  - 核心断言：`TestListPageExcludesPending`（插入 ACTIVE/DISABLED/REJECTED/PENDING 各一行，查 source=AI 返回 3 行且不含 PENDING 行的 ID）；`TestListPageKeywordEscapes`（插入 scenario 含 `%` 字面量的行，keyword=`100%` 只命中字面量行）；`TestUpdateWithVersionConflict`（错误 version 传参 RowsAffected=0 且 DB 行未变）；`TestMaxQuestionSeqIncludesSoftDeleted`（插入 Q-AG-0003 后软删，MaxQuestionSeq("Q-AG-") 返回 3）。
  - 验证命令：`cd hr-backend && go test ./internal/repository -run 'TestQuestion|TestListPage|TestUpdateWithVersion|TestMaxQuestionSeq|TestSoftDeleteWithVersion'`
  - 预期输出：ok，全部 PASS

  **specs 依据：**
  - 章节范围：§4.1.2 A（查询字段）、§4.1.2 B（显示字段）、§6.1（题目状态）
  - 业务规则索引：
    - [BR1] §4.1.2 A 关键词：按题目编号与情境描述/题项陈述全文模糊搜索
    - [BR2] §4.3.4 规则 2：待审核题目不在列表 tab 出现（恒排除 PENDING）

  **依赖：** T1
  **提交信息：** feat(questionbank): question repository 分页/乐观锁/编号序号查询

- [x] **T3: QuestionService 业务层（列表/详情/编辑/启停/删除）**

  **文件：**
  - 创建：`hr-backend/internal/service/question.go`
  - 测试：`hr-backend/internal/service/question_test.go`

  **契约：**
  - 签名：
    ```go
    // QuestionListItem 列表行 DTO（03 §3.1 响应字段）。
    type QuestionListItem struct {
        ID            string `json:"id"`
        QuestionNo    string `json:"question_no"`
        Source        string `json:"source"`
        DimensionID   string `json:"dimension_id"`
        DimensionName string `json:"dimension_name"`
        AnswerMode    string `json:"answer_mode"` // CHAT / LIKERT5，由 source 派生
        Status        string `json:"status"`
        Summary       string `json:"summary"`     // scenario 前 50 rune 截取（AI/SCALE 同规则，03 §3.1）
        UpdatedAt     time.Time `json:"updated_at"`
    }
    // QuestionDetail 详情 DTO（03 §3.2 响应字段全集，含 batch_id/batch_no/version）。
    type QuestionDetail struct { /* id/question_no/source/dimension_id/dimension_name/answer_mode/status/scenario/requirement/focus_point/reject_reason/batch_id/batch_no/reference_count/version/created_at/updated_at，json tag 按 03 §3.2 */ }
    type QuestionService interface {
        ListQuestions(ctx context.Context, in QuestionListInput) (*QuestionListResult, error)
        GetQuestion(ctx context.Context, id int64) (*QuestionDetail, error)
        UpdateQuestion(ctx context.Context, in UpdateQuestionInput) (*QuestionMutationResult, error)
        ToggleQuestionStatus(ctx context.Context, id int64, targetStatus string, version int) (*QuestionMutationResult, error)
        DeleteQuestion(ctx context.Context, id int64, version int) error
    }
    // QuestionListInput: Source string / DimensionID int64 / DimensionIDSet bool / Status string / Keyword string / Page int / PageSize int
    // UpdateQuestionInput: ID int64 / DimensionID int64 / Scenario string / Requirement string / FocusPoint string / Version int
    // QuestionMutationResult: ID string / Status string（json:"status,omitempty"，仅 Toggle 返回，Update 响应无此字段与 03 §3.3 对齐）/ Version int / UpdatedAt time.Time（Toggle 与 Update 共用；Delete 无结果）
    ```
  - 关键逻辑：
    - 列表组装：repo.ListPage 取行后批量回填 dimension_name（一次查 dimensions 需要的 ID 集合，维度已停用/软删回传存量名称：先查活跃维度 map，未命中再 Unscoped 查软删行名称，双查均无则空串）；answer_mode 按 source 派生（AI→CHAT，SCALE→LIKERT5）；summary 按 source 截前 50 rune（utf8.RuneCountInString，不用字节切片防中文截断）。
    - 编辑校验（03 §3.3）：仅 source=AI 且 status ∈ {ACTIVE, DISABLED} 可编辑，否则 1707 QuestionNotEditable；dimension_id 须命中当前启用的 AI_MGMT 维度集合（经注入的 repository.DimensionRepository.ListEnabledFullByDataSource 不够用：需按 module 过滤，扩展用 ListAll 后过滤 module_code=AI_MGMT 且 enabled），未命中 1400；scenario 1~1000 rune、requirement 1~2000 rune、focus_point 1~500 rune，越界 1400；乐观锁 RowsAffected==0 时先 FindByID 判存在性区分 1701 与 1713。
    - 启停（03 §3.4）：仅 status ∈ {ACTIVE, DISABLED} 可启停，target_status 必须与当前不同（相同 1400），非法前置 1708；乐观锁同编辑。
    - 删除（03 §3.6）：status=PENDING 返 1708（待审核题在批次中管理）；reference_count > 0 返 1705 QuestionReferenced；乐观锁；量表题删除无额外限制（释放引入位）。
    - 事务：单行更新无需跨表事务，repository 单方法原子即可。
  - 依赖：QuestionRepository、DimensionRepository（注入，列启用 AI_MGMT 维度与名称回显）；`service.NewError`；`utf8.RuneCountInString`。
  - 构造签名（含后续演进）：`NewQuestionService(repo QuestionRepository, dimRepo DimensionRepository, batchRepo QuestionBatchRepository)`。batchRepo 为监理修复追加参数：详情 batch_no 经批次仓储回填（03 §3.2），SP1 时批次表未建以空串占位、SP2 落地后回填兑现；wire 装配见 T4。

  **验收锚点：**
  - 核心断言：`TestUpdateQuestionRejectsScale`（SCALE 题编辑返回 code=1707）；`TestUpdateQuestionDimensionNotEnabled`（dimension_id 传停用维度返回 1400）；`TestUpdateQuestionLengthBounds`（scenario 恰 1000 rune 通过、1001 rune 返 1400）；`TestToggleStatusSameTarget`（当前 ACTIVE 传 target=ACTIVE 返 1400）；`TestDeleteReferenced`（reference_count=3 的题删除返 1705）；`TestDeletePending`（PENDING 题删除返 1708）；`TestListSummaryTruncates50Runes`（scenario 120 个中文字符的 AI 题 summary 长度=50）。
  - 验证命令：`cd hr-backend && go test ./internal/service -run 'TestQuestion|TestUpdateQuestion|TestToggleQuestion|TestDeleteQuestion|TestListQuestion'`
  - 预期输出：ok，全部 PASS

  **specs 依据：**
  - 章节范围：§4.1.2 A/B/D/E、§4.1.3（查看/编辑/停用启用/删除）、§4.1.4 规则 4/5/9、§2.3（鉴权矩阵）
  - 业务规则索引：
    - [BR1] §4.1.4 规则 4：被引用次数>0 不可删除，只可停用
    - [BR2] §4.1.4 规则 5：量表题任何界面不提供文本编辑能力
    - [BR3] §4.1.2 E 情境描述必填 (1,1000)，作答要求 (1,2000)，考察点 (1,500)
    - [BR4] §4.1.2 E 所属维度改选限当前启用集合
    - [BR5] §4.1.4 规则 9 并发冲突（乐观锁）返 1713
    - [BR6] §4.1.3 删除：待审核题目在批次中管理不可删（返 1708）

  **依赖：** T2
  **提交信息：** feat(questionbank): question service 校验/乐观锁/摘要截断

- [x] **T4: errcode 17xx 段 + QuestionHandler + 路由 + wire 装配**

  **文件：**
  - 修改：`hr-backend/internal/pkg/errcode/errcode.go`（一次性定义 1701-1709 + 1713 全量常量与 messages，段位注释同步）
  - 创建：`hr-backend/internal/api/handler/question.go`
  - 测试：`hr-backend/internal/api/handler/question_test.go`
  - 修改：`hr-backend/internal/api/router/router.go`（auth 组挂 5 路由）
  - 修改：`hr-backend/wire.go`（wire.Build 追加 repository.NewQuestionRepository / service.NewQuestionService / handler.NewQuestionHandler）后 `go generate ./...` 再生 wire_gen.go。装配演进：service 构造现为三参（追加 batchRepo，见 T3）；handler 构造现为三参（追加 batchSvc 承载重新提交归批，SP2 接管，见 02 子计划）

  **契约：**
  - 签名：
    ```go
    // 错误码常量（errcode.go 追加，03 §4.1 全量）：
    // QuestionNotFound=1701 / QuestionBatchNotFound=1702 / GenerationNotFound=1703 /
    // ScaleAlreadyImported=1704 / QuestionReferenced=1705 / QuestionBatchClosed=1706 /
    // QuestionNotEditable=1707 / QuestionStatusInvalid=1708 / LLMNotConfigured=1709 /
    // QuestionVersionConflict=1713
    func NewQuestionHandler(svc service.QuestionService, dimSvc service.DimensionService, batchSvc service.QuestionBatchService) *QuestionHandler
    // dimSvc 为构造契约预留形参（本子计划未消费）；batchSvc 承载重新提交归批
    // （03 §3.5，SP2 落地接管），行级维护与归批分属两个 service
    // QuestionHandler 方法（路由见下）：
    // List(c *gin.Context)    GET /api/questions?source=&dimension_id=&status=&keyword=&page=&page_size=
    // Detail(c *gin.Context)  GET /api/questions/:id
    // Update(c *gin.Context)  POST /api/questions/update
    // ToggleStatus(c *gin.Context) POST /api/questions/toggle-status
    // Delete(c *gin.Context)  POST /api/questions/delete
    ```
  - 关键逻辑：handler 薄壳：ShouldBindJSON 失败返 1400；ID 字段（string）经 parseID 转 int64，格式错 1400；source 参数缺省/非法（非 AI/SCALE）1400；service 返回统一走 handleServiceError。注意路由顺序：`auth.GET("/questions/:id", ...)` 与 `/questions` 静态路由不冲突（Gin 静态优先）。
  - 依赖：T3 的 service；response/errcode 包；router 现有结构。

  **验收锚点：**
  - 核心断言：`TestQuestionHandlerListMissingSource`（GET /api/questions 无 source 返回 HTTP 200 + code 1400）；`TestQuestionHandlerDetailBadID`（/api/questions/abc 返 1400）；`TestQuestionHandlerUpdateMapsVersionConflict`（service 返回 1713 时 body code=1713 且 HTTP 200）。
  - 验证命令：`cd hr-backend && go generate ./... && go build ./... && go test ./internal/api/handler -run TestQuestion`
  - 预期输出：wire_gen.go 再生成功、build 无错、测试 PASS

  **specs 依据：**
  - 章节范围：§2.3（接口鉴权矩阵：列表/详情/编辑/启停/删除五行）、§4.1.4 规则 9
  - 业务规则索引：
    - [BR1] §2.3 接口鉴权矩阵：questions 五接口全 JWT，错误业务码 HTTP 200 带 code
    - [BR2] §4.1.4 规则 9：参数校验失败 1400，并发冲突 1713

  **依赖：** T3
  **提交信息：** feat(questionbank): questions 五接口 handler/路由/wire 装配与 17xx 错误码

- [x] **T5: 前端契约类型与 question-bank feature api/hooks**

  **文件：**
  - 修改：`hr-frontend/src/lib/contracts.ts`（新增 QuestionListItem/QuestionDetail/QuestionListPage/UpdateQuestionPayload/ToggleQuestionStatusPayload/DeleteQuestionPayload 与 ErrCode 1701-1713）
  - 创建：`hr-frontend/src/features/question-bank/api.ts`、`hr-frontend/src/features/question-bank/types.ts`、`hr-frontend/src/features/question-bank/hooks.ts`
  - 测试：`hr-frontend/src/features/question-bank/__tests__/hooks.test.tsx`

  **契约：**
  - 签名：
    ```ts
    // contracts.ts（ID 一律 string，snake_case 直译后端）
    export interface QuestionListItem {
      id: string; question_no: string; source: 'AI' | 'SCALE';
      dimension_id: string; dimension_name: string;
      answer_mode: 'CHAT' | 'LIKERT5';
      status: 'ACTIVE' | 'DISABLED' | 'REJECTED';
      summary: string; updated_at: string;
    }
    export type QuestionListPage = Page<QuestionListItem>;
    // api.ts：
    export async function fetchQuestions(params: QuestionQuery): Promise<QuestionListPage>
    export async function fetchQuestionDetail(id: string): Promise<QuestionDetail>
    export async function updateQuestion(payload: UpdateQuestionPayload): Promise<QuestionMutationResult>
    export async function toggleQuestionStatus(payload: ToggleQuestionStatusPayload): Promise<QuestionMutationResult>
    export async function deleteQuestion(payload: DeleteQuestionPayload): Promise<void>
    // hooks.ts：
    export function useQuestions(query: QuestionQuery): UseQueryResult<QuestionListPage>
    export function useQuestionDetail(id: string | null): UseQueryResult<QuestionDetail>
    export function useUpdateQuestion(): UseMutationResult<...>
    export function useToggleQuestionStatus(): UseMutationResult<...>  // onSuccess invalidate ['question-bank','questions']
    export function useDeleteQuestion(): UseMutationResult<...>        // 同上
    ```
  - 关键逻辑：queryKey `['question-bank', 'questions', query]`（对象引用稳定性由调用方 useMemo 保证，batch-table 先例）；mutation onSuccess `invalidateQueries({ queryKey: ['question-bank'] })`；enabled 守卫 `!!token`。types.ts 定义 `QuestionTab = 'AI' | 'SCALE'` 与 `QuestionQuery`（source 为必填，tab 与 source 一一映射）。
  - 依赖：httpClient（已解包）、contracts Page、useAuthStore；维选项数据复用 `useDimensionTree`（features/dimension/hooks.ts，树内按 module_code 过滤）。

  **验收锚点：**
  - 核心断言：hooks 测试（vitest + QueryClientProvider 包装）：mock api 模块后 `useQuestions` 初次渲染触发 fetchQuestions 且参数 snake_case 透传（page_size 非 pageSize）；`useDeleteQuestion` 成功后调用 queryClient.invalidateQueries（以 spy 断言）。
  - 验证命令：`cd hr-frontend && pnpm type-check && pnpm test -- question-bank`
  - 预期输出：tsc 无错，vitest 该目录测试 PASS

  **specs 依据：**
  - 章节范围：§4.1.2 A（查询字段）、§4.1.3（功能与按钮的服务层对接）
  - 业务规则索引：
    - [BR1] §4.1.2 A 查询字段：维度/状态/关键词三条件 + 分页，source 必选对应 tab

  **依赖：** T4（接口可用性）
  **提交信息：** feat(question-bank): 前端契约类型与 api/hooks 层

- [x] **T6: 题库管理页列表态（双 tab + 查询 + 表格 + 行操作弹窗）**

  **文件：**
  - 创建：`hr-frontend/src/features/question-bank/components/question-table.tsx`、`question-view-dialog.tsx`、`question-form-dialog.tsx`
  - 创建：`hr-frontend/src/routes/_authenticated/question-bank/index.tsx`
  - 修改：`hr-frontend/src/components/nav-menu.tsx`（F5 占位接 `to: '/question-bank'`）
  - 修改：`hr-frontend/src/i18n/locales/zh.json`、`en.json`（questionBank ns 全量文案）
  - 测试：`hr-frontend/src/features/question-bank/components/__tests__/question-table.test.tsx`、`question-form-dialog.test.tsx`、`question-view-dialog.test.tsx`

  **契约：**
  - 签名：
    ```tsx
    // question-table.tsx：单 tab 列表组件，双 tab 各持一份实例（specs 4.1.5 查询条件/分页独立保持）
    export interface QuestionTableProps {
      tab: QuestionTab;             // 'AI' | 'SCALE'，决定维度下拉数据源与操作列按钮
      onEdit: (q: QuestionListItem) => void;      // 打开编辑弹窗（仅 AI 题）
      onView: (q: QuestionListItem) => void;      // 查看弹窗
    }
    export function QuestionTable({ tab, onEdit, onView }: QuestionTableProps): JSX.Element
    // question-form-dialog.tsx：编辑弹窗（RHF+Zod，zodResolver；mode 固定 'edit'，预载详情回填）
    export interface QuestionFormDialogProps { open: boolean; onOpenChange: (o: boolean) => void; question: QuestionDetail | null; onSaved: () => void }
    // question-view-dialog.tsx：查看弹窗，底部「编辑」按钮仅 source=AI 且 status∈{ACTIVE,DISABLED} 显示
    ```
  - 关键逻辑：
    - 双 tab：页面级 useState `activeTab`，两 QuestionTable 常驻挂载、隐藏非激活 tab（保查询状态）或各自持有 filter state 由父页管理，二选一由实现定，验收锚点是切 tab 不清空另一 tab 条件；tab 头显示 `AI 管理题库（共 N 题）/ 九型人格量表（共 N 题）`（N 取列表 total，未加载时省略数）。
    - 操作列按状态渲染（specs 4.1.3）：AI 题 ACTIVE/DISABLED → 查看/编辑/停用或启用/删除；AI 题 REJECTED → 查看/重新提交（本计划占位禁用按钮，SP2 T6 接通）/删除；SCALE 题 → 查看/停用或启用/删除（无编辑、无重新提交）。作答方式列仅 SCALE tab 显示（LIKERT5）。
    - 删除：ConfirmDialog（destructive）确认后 mutate，ApiError code 1705 时 toast「该题已被测试引用，只可停用」，1713 时 toast「数据已变更，请刷新后重试」并 refetch。
    - 编辑弹窗 Zod schema：维度 enum（当前启用 AI_MGMT 集合）、scenario min1 max1000、requirement min1 max2000、focus_point min1 max500，校验文案走 `t()`；保存成功 toast 留列表态。
    - 空状态：tab 无数据时引导文案 + 内嵌按钮（AI tab「生成 AI 管理题」跳 /question-bank/generate 占位路由禁用至 SP4、SCALE tab「引入九型量表」按钮禁用至 SP3，均渲染但 disabled + tooltip 说明待后续功能）。
    - 页面级按钮「生成 AI 管理题」「引入九型量表」同上 disabled 占位。
    - i18n：questionBank ns 覆盖 tab 名/列名/状态标签（启用/已停用/已驳回）/来源标签/按钮/弹窗文案/toast，zh 与 en 同步。
  - 依赖：T5 hooks、ui/dialog、ConfirmDialog、useDimensionTree（AI_MGMT 模块 enabled 子集；SCALE tab 型别维度空集时下拉只有「全部维度」）、react-hook-form+zodResolver、sonner toast。

  **验收锚点：**
  - 核心断言：question-table 测试：SCALE tab 渲染的行无「编辑」按钮（queryByRole('button', { name: 编辑 }) 为 null）；REJECTED 的 AI 行有「重新提交」且 disabled；切换查询条件点「查询」才发请求（draft→filter 两段式）；question-form-dialog 测试：超长 scenario（1001 字）提交被 Zod 拦截且错误文案可见、保存成功调 onSaved；view-dialog 测试：REJECTED 题显示驳回原因区块、底部无「编辑」按钮。
  - 验证命令：`cd hr-frontend && pnpm type-check && pnpm test`
  - 预期输出：tsc 无错，全量 vitest PASS（含既有测试无回归）

  **specs 依据：**
  - 章节范围：§4.1.1（页面职责）、§4.1.2 A/B/D/E、§4.1.3（题库查询/查看/编辑/停用启用/删除五功能）、§4.1.4 规则 4/5/9、§4.1.5（交互逻辑）、§3.2（跳转流程）、§4A.1/4A.4（线框）
  - 业务规则索引：
    - [BR1] §4.1.5 双 tab 各自独立保持查询条件、页码与每页条数，切 tab 不互相清空
    - [BR2] §4.1.4 规则 4：删除被引用题前端提示「该题已被测试引用，只可停用」
    - [BR3] §4.1.4 规则 5：量表题行内无「编辑」与「重新提交」按钮
    - [BR4] §4.1.4 规则 9：并发冲突 toast「数据已变更，请刷新后重试」并刷新列表
    - [BR5] §4.1.5 列表默认按更新时间倒序，摘要过长截断悬浮展示
    - [BR6] §4.1.2 B 量表 tab 显示「作答方式 Likert 5 级」列，AI tab 无此列

  **依赖：** T5
  **提交信息：** feat(question-bank): 题库管理页双 tab 列表与查看/编辑/启停/删除弹窗

- [x] **T7: CLAUDE.md 状态同步（子计划收尾）**

  **文件：**
  - 修改：`CLAUDE.md`（根，未开工清单去掉 questionbank 一句）、`hr-backend/CLAUDE.md`（路由清单加 questions 五接口、域状态）、`hr-frontend/CLAUDE.md`（feature 清单加 question-bank、路由与菜单接通）

  **步骤：** 按项目「规约文档随实现同步」约束，本子计划合入的域状态、路由清单、错误码段（17xx）落进三份 CLAUDE.md，与代码同一提交集。

  **验收锚点：**
  - 验证命令：git diff 检查三份 CLAUDE.md 变更段落存在
  - 预期输出：questionbank 域描述与实际路由一致

  **specs 依据：**
  - 章节范围：无（纯文档同步任务，对应实现任务的 specs 依据已在 T1-T6 列出）
  **依赖：** T6
  **提交信息：** docs: CLAUDE.md 同步 questionbank 题库台账域状态
