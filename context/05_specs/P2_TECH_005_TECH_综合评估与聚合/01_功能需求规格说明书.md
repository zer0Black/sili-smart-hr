# 综合评估与聚合 - 技术组件规格说明书

## 文档信息

- 基线 Feature：无
- 变更组：无

---

# 技术组件规格说明书

## 1. 概述

### 1.1 组件名称

综合评估与聚合（Assessment Evaluator / Scorer / Activity）

### 1.2 组件描述

engine 的三个计算子域：evaluator 读单人周期内全部会话特征档案，组装 prompt 一次性调 LLM 评完对话分析全部启用维度（0-100 整数分加脱敏理由）；scorer 把维度分按权重聚合为模块分与总览分（数据不足维度标记 insufficient 并从聚合中剔除）；activity 纯规则统计会话频率与轮次，按有效会话数分级活跃/低频/未使用，不调 LLM。本组件产出到评分与聚合结果落库为止，个人画像快照生成归 F6 评估运营域（消费本组件落库数据二次汇聚）。

### 1.3 创建背景

T4 会话特征抽取已把原始对话压缩为脱敏特征档案（session_features 表），PRD 技术泳道 T5 是档案到评分的最后一公里：单人周期内全部档案须一次性喂给 LLM 综合评估（跨会话证据互证，逐会话评分既贵又割裂），评出的维度分要按维度配置的权重聚合成模块分与总览分，使用活跃度作为能力评分的基线要纯规则统计。三者共用一次取数、一次落库，共同构成周期跑批的计算内核，被 T6 pipeline 编排驱动，周期批量与手动定向分析复用同一套处理器。

上游实证（2026-09-05 抽样，本周评估窗口 2026-08-30 23:00 起）：窗口会话总量 14078，最活跃单人窗口内 143+ 会话且 turn_count=1 占比约六成，与 T4 抽取规格披露的工作型 tc=1 / 自动化客户端人群签名吻合。单档案千字符级（T4 设计约束），最活跃人单周期档案集可达 100k+ 字符，evaluator 输入必须做预算控制，这是本组件档案集分层组装能力的直接动因。

T4 规格附录 7.2 留下 C01-C13 消费侧约束清单（证据稀薄误判、人群签名识别、降权语义等），本组件必须逐条承接，承接映射见 2.4 能力1 与附录 7.2。

### 1.4 技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| Go | 1.25 | 实现语言，与 hr-backend 一致，module sili-smart-hr/backend |
| GORM | v2 | 评分、聚合、活跃度三表落库（TEXT 列存序列化 JSON，多库兼容 tag 约定同 T4） |
| LLM 调用底座（P2_TECH_001） | 已实现 | 综合评估的唯一 LLM 通道（并发 4、重试、token 预算由底座承载） |
| 会话日志集成（P2_TECH_002） | 已实现 | 活跃度统计的会话列表拉取通道 |
| 会话特征抽取（P2_TECH_003/004） | 已实现 | 特征档案唯一来源（ListByPersonAndRange 取数） |
| 维度与权重配置（P2_DIM_001） | 已实现 | 评分提示词、评分锚点、聚合权重、活跃度阈值的配置来源 |
| Asynq | 已实现 | 任务承载：一人一任务，由 T6 编排调度 |

---

## 2. 组件接口

### 2.1 能力清单

| 序号 | 能力名称 | 能力类型 | 说明 |
|------|---------|---------|------|
| 1 | 跨会话综合评估 | 基础 | 读单人周期全部特征档案，组装评分 prompt 一次调 LLM 评完对话分析全部启用维度，产出各维 0-100 整数分、脱敏理由与证据充足性标记；零有效档案时不调 LLM 直接落全维度降权行 |
| 2 | 档案集分层组装 | 基础 | 档案集超预算时的输入控制：周期统计汇总块（规则聚合，全量必进）+ 逐会话档案块（按证据密度降序、预算内截取），prompt 内披露可见档案占比 |
| 3 | 使用活跃度统计 | 基础 | 纯规则统计会话列表与档案表 status 分布，产出活跃/低频/未使用分级（基于有效会话数口径）与人群签名标注，不调 LLM |
| 4 | 人群签名识别 | 基础 | 承接 T4 C08-C12：按列表口径与档案 status 分布（含 client 列）识别自动化客户端、旁路族群、阈值误杀三类签名，输出 evidence_note 标注与处置（活跃度口径修正或数据不足标注），不作硬拦截 |
| 5 | 多维打分与聚合 | 基础 | 维度分按权重聚合为模块分与总览分（insufficient 维度剔除、剩余权重归一化），聚合口径统一服务对话分析与主动测试评分，幂等重算 |
| 6 | 单人周期评估原子入口 | 基础 | 供 T6 编排的一人一次组合入口：活跃度统计 → 综合评估 → 聚合落库，含幂等（已 success 跳过、failed 翻转），周期批量与手动定向分析复用 |

> **能力类型说明**：基础=核心必需能力 | 高级=增强型能力 | 扩展=可选能力

### 2.2 输入定义

#### Props / 参数列表

方法级参数：

| 参数名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| ctx | context.Context | 是 | - | 超时与取消控制；EvaluatePerson 的 ctx 预算分路径：sessions 非空 ≥ 900s（LLM 段最坏 840s + 档案取数与组装余量）、sessions 空 ≥ 1050s（StatPersonByKey 列表拉取最坏 190s + LLM 段 840s + 余量，03 §3.3 口径），由调用方（T6 任务）赋予，本组件不再叠加更短超时 |
| tokenName | string | 是 | - | 人员标识（上游调用令牌名），档案归属键，喂 LLM 前剥离 |
| period | Period | 是 | - | 评估区间，闭开区间 [Start, End)，Unix 秒；周期批量取系统配置的跑批窗口，手动定向分析取运营选定时段，均由调用方传入 |
| sessions | []conversationlog.SessionSummary | 否 | - | 会话列表入参，两个消费方：活跃度统计（StatPerson）与签名识别（Evaluate 内 IdentifyPopulation）。EvaluatePerson 空时内部经 StatPersonByKey 拉取后注入两处；Evaluate 空时不拉取，签名识别退化（见 2.4 能力1 注意事项） |

#### 配置项说明

```go
// 评估行为常量，包内定义，调整走代码变更发版。
const (
    MinValidProfilesForEval = 1     // 有效档案数下限：周期内 success 档案数 < 此值时跳过 LLM，
                                    // 落全维度 insufficient 评分行（零有效档案人群：
                                    // 未使用、旁路族群、自动化客户端，见 2.4 能力4）
    MaxProfileSetTokens = 30000     // 档案集 token 预算（evaluator 单人输入，非用户内容）
    MaxProfileSetChars  = MaxProfileSetTokens * 3 // 字符执行口径（1 token ≈ 3 字符，与 T4 同折算）：
                                    // 周期统计汇总块必进不占此预算判定（紧凑数字，千字符级），
                                    // 逐会话档案块（三块文本）合计超此值按证据密度截取
    MaxRationaleChars = 200         // 单维度评分理由字数约束，写在 prompt 模板文本内（模板为包内
                                    // 字符串常量，随模板版本化），落库校验容差 2 倍（超
                                    // MaxRationaleChars×2 判 schema 失败走重试），与 T4 摘要
                                    // 约束的承载机制同构
    ScoreMin = 0                    // 绝对分下界（0-100 整数分制，锚点区间约定见维度配置）
    ScoreMax = 100                  // 绝对分上界
)

// 活跃度分级阈值经维度配置读取（P2_DIM_001 活跃度规则：活跃判定下限默认 10、
// 低频判定下限默认 5），本组件不重复定义默认值，运行时从 dimension 域读取。

// Period 是评估区间（闭开区间，Unix 秒）。
type Period struct {
    Start int64 // 含
    End   int64 // 不含
}

// DimensionSpec 是参与评分的维度口径快照（从 dimension 域读取启用维度组装）。
// 取数时快照落评分行，保证历史评分可追溯（配置变更下次跑批生效，历史不重算）。
type DimensionSpec struct {
    Code         string  // 维度编码（唯一键，如 AI_INSTRUCTION）
    Name         string  // 维度名称
    Module       string  // 所属模块（AI 使用能力 AI_USAGE；聚合按模块分组，值域同 DIM module_code）
    PromptText   string  // 评分提示词（仅对话分析维度非空）
    AnchorText   string  // 评分锚点（0-100 分值区间对应能力表现）
    Weight       int     // 模块内聚合权重百分比（0-100）
    InOverview   bool    // 是否参与聚合（模块分与总览分均以此为门槛）
}

// ProfileDigest 是从 session_features 行解析的档案消费视图（T4 FeatureProfile 的
// 消费侧裁剪：本组件只读四块内容与 status，不感知抽取内部结构）。
type ProfileDigest struct {
    SessionKey string
    Status     string          // success / failed / skipped
    Client     string          // 客户端标识（T4 分层探测落库列，人群签名维度）
    Stats      ProfileStats    // 统计块（success 与 failed 行均有；skipped 行零值）
    HasBlocks  bool            // success 行为 true（Summary/Instruction/Behavior 三块可用）
    ProfileJSON string        // 原始档案 JSON（success 行），仅内存临时持有
}
```

#### 评分 prompt 组装规则（C01-C13 承接映射）

prompt 模板为包内字符串常量（系统段 + 维度段 + 证据段 + 统计段 + 指令段），证据段与统计段的组装规则逐条承接 T4 消费侧约束：

| T4 约束 | 承接设计（组装进统计参考块或 prompt 指令） |
|---------|------------------------------------------|
| C01 零输入会话指令稀薄 | 统计块含 UserMsgCount 合计与零输入会话数；prompt 指令明确：指令类维度证据稀薄时标 insufficient，禁止把证据不足判为低分 |
| C02 narrative_absent 可信度降级 | 统计块含 narrative_absent 会话占比；价值产出与审查把关维度按占比作证据降权参考 |
| C03 工具证据粒度 | prompt 披露：工具证据仅达工具名与字符数级别，工作域归纳精度有限 |
| C04 同首指令哈希按重做 | prompt 指令：CmdReuseHashes 同首指令命中一律按重做/重试解读，不作知识沉淀复用证据计分，不设间隔条件 |
| C05 粘贴量联合判读 | 统计块含 PasteCharCount 合计、发生粘贴的会话数（PasteCharCount>0 的档案行数，数据源仅档案行级计数，无消息条次计数）、UserMsgCount 三者联合呈现；prompt 指示单一事件主导的粘贴量不读作持续高约束供给习惯 |
| C06 failed 行参与降权 | 统计块含 failed 档案数；该部分会话仅统计块可用，LLM 段证据缺失 |
| C07 续接会话回顾文本 | 统计块含 continuation 会话数；prompt 指示续接推进会话的叙述混有上会话回顾，指令与价值判读联合识别防双重计数 |

人群签名类约束 C08-C12 的承接在能力4（规则侧），不进 prompt。

### 2.3 输出定义

#### Events / 返回值

| 方法名 | 参数 | 返回值 | 说明 |
|--------------|------|--------|------|
| EvaluatePerson | ctx, tokenName, period, sessions | (*EvaluateResult, error) | 单人周期评估原子入口（能力6）：活跃度统计 → 综合评估 → 聚合，一次完成三表落库 |
| Evaluate | ctx, tokenName, period, sessions | (*EvaluateResult, error) | 仅综合评估（能力1）：读档案、签名识别、组装、调 LLM、评分行落库，不含活跃度与聚合。幂等判定内聚在本方法：周期匹配按 period_start 与 period_end 均精确等于传入 period（与 Aggregate 同口径），既有全维度 success 行跳过 LLM（Reused=true），既有 failed 行先删后评（唯一索引防撞，语义同能力6 规则1/2） |
| AssembleProfileSet | ctx, tokenName, period | (*ProfileSet, error) | 档案集分层组装（能力2）：纯读不调 LLM 不落库，供调试与测试 |
| StatPersonByKey | ctx, tokenName, period | (*ActivityStat, error) | 活跃度统计（能力3）：内部经 P2_TECH_002 拉列表后统计并落库 |
| StatPerson | ctx, sessions, tokenName, period | (*ActivityStat, error) | 活跃度统计（列表由调用方传入形态，T6 已拉列表时免二次拉取） |
| IdentifyPopulation | sessions, profiles | PopulationSignature | 人群签名识别（能力4）：纯函数，签名枚举 + evidence_note 文案键 |
| Aggregate | ctx, tokenName, period | (*AggregateResult, error) | 多维打分与聚合（能力5）：读该人该周期全部评分行（period_start 与 period_end 均精确等于传入 period 的行，含 F7 阅卷写入的主动测试维度）重算聚合行，幂等 |

```go
// EvaluateResult 是单人周期评估的组合结果。
type EvaluateResult struct {
    Activity  *ActivityStat     // 活跃度统计行（EvaluatePerson 路径产出）
    Scores    []DimensionScore  // 维度评分行（含 insufficient 标记行）
    Aggregate *AggregateResult  // 聚合结果（EvaluatePerson 路径产出）
    Skipped   bool              // true 表示跳过 LLM 直接落全维度 insufficient 评分行（零有效档案，或签名命中 bypass/auto_client 且有效档案数达下限，见能力1 流程段与能力4 处置列）
    Reused    bool              // true 表示命中既有 success 评分行复用未调 LLM（幂等，见能力6）
}

// DimensionScore 对应评分记录表 dimension_score（关键列，DDL 细节归 04 接口数据设计）：
// id（雪花）、token_name、period_start、period_end、dimension_code、module、
// score（int，insufficient 时落 0）、rationale（TEXT，脱敏理由）、insufficient（bool）、
// evidence_json（TEXT：周期 success 档案 session_key 清单 + 统计摘要快照 +
// 维度口径摘要（weight、InOverview，聚合唯一权重来源），规则产出
// 全量追溯，不用 LLM 引用防幻觉）、source（conversation/active_test）、model_name、
// prompt_version、status（success/failed）、error_code、created_at。
// 唯一索引 (token_name, period_start, dimension_code)，不含 source：维度数据来源为
// DIM 配置单选（一维度恒属一 source），conversation 与 active_test 的 dimension_code
// 集合恒不相交，索引无需 source 列即天然防撞。
type DimensionScore struct {
    DimensionCode string
    Module        string
    Score         int
    Rationale     string
    Insufficient  bool
    EvidenceJSON  string
    Source        string // conversation（本组件）/ active_test（F7 阅卷）
    Status        string // success / failed
}

// AggregateResult 对应聚合结果表 aggregate_score（关键列）：id（雪花）、token_name、
// period_start、period_end、module、module_score（float，可空）、overview_score（float，
// 仅总览行落值）、included_json（参与聚合维度与权重快照）、excluded_json（被剔除的
// insufficient 与 failed 维度清单）、created_at。唯一索引 (token_name, period_start, module)。
// 总览行为独立行，module 落哨兵值 "overview"，module_score 为空；各业务模块行
// overview_score 为空。
// IncludedWeight 是参与聚合维度的权重快照项（included_json 的序列化单元，
// 权重取评分行 evidence_json 口径摘要）。
type IncludedWeight struct {
    Code   string
    Weight int
}

type AggregateResult struct {
    ModuleScores map[string]*float64          // 模块分（模块编码 → 加权平均，全剔除时 nil）
    Overview     *float64                     // 总览分（全部参与维度的加权平均，跨模块直聚合）
    Included     map[string][]IncludedWeight  // 模块 → 参与聚合维度编码与权重快照
    Excluded     map[string][]string          // 模块 → insufficient 与 failed 维度剔除清单
}

// ActivityStat 对应活跃度表 activity_stat（关键列）：id（雪花）、token_name、
// period_start、period_end、session_count（列表口径）、valid_session_count、
// skipped_count、total_turns、active_level（active/low_freq/unused）、
// population_note（人群签名标注文案键，见能力4）、client_dist_json（按档案行
// client 列聚合的分布；列表 SessionSummary 无 client 字段，此列是档案行口径，
// 三态行均含 client 列，detail_invalid 跳过行为空串例外，T4_004 契约）、created_at。唯一索引 (token_name, period_start)。
type ActivityStat struct {
    SessionCount     int
    ValidSessionCount int
    SkippedCount     int
    TotalTurns       int
    ActiveLevel      string // active / low_freq / unused
    PopulationNote   string // 空串=正常人群；枚举见能力4（work_tc1 落独立文案键，画像无特殊呈现）
    ClientDist       map[string]int
}

// ProfileSet 是档案集分层组装产物（喂 LLM 的输入视图，仅内存临时持有）。
type ProfileSet struct {
    SummaryBlock  string   // 周期统计汇总块（规则聚合全部档案 Stats，全量必进）
    SessionBlocks []string // 逐会话档案块（success 档案按证据密度降序、预算内截取）
    VisibleCount  int      // 进视图的档案数
    TotalSuccess  int      // 周期 success 档案总数（截断披露：VisibleCount/TotalSuccess）
}
```

#### 错误码定义

| 错误码 | 说明 | 处理建议 |
|--------|------|---------|
| ErrNoDimensions | 无启用的对话分析维度（dimension 配置缺失或全部停用） | 评估无法运行的配置前置问题：不落评分行，error 上抛交 Asynq 任务级重试（配置可能在线修复，重试有意义），耗尽后由 T6 记 ERROR，运营到维度配置页补齐 |
| ErrLLMEvalUpstream | LLM 评估调用失败（底座重试耗尽，含超时/排队满/限流耗尽态） | 落 failed 评分行（全维度占位，error_code 记因），当期聚合照常推进（该周期无对话分析维度分），补跑时重评。error_code 落库值域：ErrLLMEvalUpstream（本码）与 ErrSchemaInvalid 之外，另承载底座细分哨兵 llm.ErrContextLengthExceeded（token 预算超限的确定性失败，细分记因便于与一般上游失败区分观测） |
| ErrSchemaInvalid | LLM 输出未通过评分 schema 校验（重试一次仍失败） | 同 ErrLLMEvalUpstream 落 failed 行，补跑重评；观测指标 eval_schema_fail_ratio 集中抬升时核查当前启用模型 |
| ErrNoValidProfiles | 周期内零有效档案（success 数 < MinValidProfilesForEval） | 业务态非错误：跳过 LLM，落全维度 insufficient 评分行 + 活跃度行（人群签名 note），Evaluate 返回 Skipped=true 且 err=nil。此码仅作文档语义标识，运行时走 Skipped 标记通道，error 通道不承载；跳过路径落的 success 行 error_code 列带 skip_no_llm 标记（幂等复用排除标记，见能力6 规则1） |
| ErrDimensionConfigRead | 维度配置或活跃度阈值读取失败 | 配置域数据访问异常：error 上抛交 Asynq 任务级重试，不落评分行 |
| ErrProfileRead | 档案表读取失败（ListByPersonAndRange 数据访问异常） | 数据访问异常：error 上抛交 Asynq 任务级重试，不落评分行 |
| ErrSessionListFetch | 会话列表拉取失败（StatPersonByKey 内部经 P2_TECH_002 拉取重试耗尽，上游错误分类透传记因） | 数据访问异常：error 上抛交 Asynq 任务级重试，不落任何行；调用方已传 sessions 的路径（StatPerson / EvaluatePerson 带列表）不触发此码 |
| ErrStoreWrite | 评分/聚合/活跃度落库失败 | 不落行，error 上抛交 Asynq 任务级重试；重试耗尽该人本周期无评分行，属 eval_fail_ratio 分子已知漏计项（观测替代见 6.2） |
| ErrScoreRead | 既有评分行读取失败（Aggregate 聚合读数与能力6 幂等判定路径） | 数据访问异常：error 上抛交 Asynq 任务级重试；与 ErrProfileRead（档案表读取）对称定义，ErrStoreWrite 专属落库方向 |

### 2.4 能力详细说明

#### 能力1: 跨会话综合评估

**描述**: 单人周期一次 LLM 调用评完全部对话分析维度。流程：读维度口径快照（启用 + 数据来源=对话分析的维度，含提示词与锚点）→ ListByPersonAndRange 取档案（取数窗口放宽缓冲：start 前移 7 天，容纳 first_turn 在前周期、last_turn 落本周期的跨边界档案行；取回后按 last_turn_time ∈ [Start, End) 内存二次过滤，末轮归属与活跃度口径对齐。缓冲源于上游库按 first_turn_at 过滤的既定契约，7 天覆盖周周期内任意跨界形态）→ 人群签名识别（能力4）→ 零有效档案走 ErrNoValidProfiles 业务态 → 签名命中 bypass_orchestrator / auto_client 且有效档案数 ≥ MinValidProfilesForEval 时跳过 LLM 走全维度 insufficient 落库（处置见能力4 处置列，与零档案路径同型）→ 档案集分层组装（能力2）→ 组装 prompt → 经 LLM 调用底座调用当前排他启用模型 → schema 校验（失败重试一次，宽容解析同 T4：剥 markdown 围栏、截取首个含 dimensions 键的顶层平衡 JSON）→ 逐维度落评分行（rationale 落库前过 Redact 兜底脱敏，防御纵深）。

prompt 五段结构：系统段（人才能力评估评分员角色 + 输出 JSON schema 约束）、维度段（逐维度：名称 + 评分提示词 + 评分锚点，来自维度配置原文）、证据段（ProfileSet 逐会话块，每块带 session_key 标签）、统计段（ProfileSet 汇总块 + C01-C07 承接数据，见 2.2 组装规则表）、指令段（0-100 整数分、证据不足标 insufficient 且 score 置 null、理由 ≤200 字、同首指令按重做解读等判定纪律）。TokenName 组装前剥离，人员归属落库时回填，人名不进 LLM 上下文。

**评分输出 schema**（LLM 按 JSON 输出，逐维度）：

```json
{
  "dimensions": [
    {"code": "AI_XXX", "score": 72, "insufficient": false, "rationale": "维度评分理由（≤200字）"},
    {"code": "AI_YYY", "score": null, "insufficient": true, "rationale": "证据不足说明"}
  ]
}
```

schema 校验规则：code 须命中维度段集合（多出的丢弃、缺失的按 insufficient 补行）；score 非 null 时须为 0-100 整数，宽容解析容忍浮点整数形态（78.0 收敛 78，OpenAI 兼容通道无严格 integer schema 约束时的常见输出），带小数部分（78.5）判 schema 失败；rationale 超 MaxRationaleChars×2 判失败。校验通过后，缺失维度与 insufficient=true 维度统一落 insufficient 行（score 落 0、标记 true、status=success，failed 状态专属 LLM 失败路径，零档案跳过路径落的 insufficient 行同为 success），理由取 LLM 输出或配置的缺省文案。

**接口定义**:
```go
// Evaluate 对单人周期执行综合评估（不含活跃度与聚合）。
// sessions 可空：非空时直传签名识别（空时签名识别退化为仅档案侧判据，
// 不做单人列表拉取，见下方说明）。幂等判定内聚：全 success 复用跳过
// LLM、failed 行先删后评（见 2.3 返回值表与能力6 规则1/2）。
func (e *Evaluator) Evaluate(ctx context.Context, tokenName string, period Period, sessions []conversationlog.SessionSummary) (*EvaluateResult, error)
```

**调用示例**:
```go
res, err := ev.Evaluate(ctx, "李雪涛", Period{Start: weekStart, End: weekEnd}, sessions)
if err != nil { return err }   // 配置读取/落库失败交任务重试
if res.Skipped {
    slog.Info("eval skipped: no valid profiles", "token", tokenName)
    return nil
}
```

**注意事项**:
- sessions 入参是签名识别的列表口径数据来源（tc=1 占比、列表会话量门槛判据）：组合路径由 EvaluatePerson 注入；独立调用（调试/测试）传空时签名识别退化为仅档案侧判据（status 分布与 client 列），列表量门槛对空列表同样生效、落 normal（判定次序设计段 v1.1 澄清）；中文 token_name 上游过滤不可用（T4 3.2），Evaluate 不做单人全量拉列表兜底
- 成本收敛：严格每人周期一次 LLM 调用；「会话量小的周期合并抽取与综合评估为一次调用」的优化属 T6 pipeline 编排范围（架构 3.1.2 成本控制条款），本组件保持接口独立
- 评分口径快照：DimensionSpec 在取数时快照进评分行（evidence_json 附维度口径摘要，含 weight 与 InOverview，是聚合的唯一权重来源），维度配置变更后已产出历史不重算（与 DIM 规格规则1 配置生效时机一致）；F7 阅卷写入的 active_test 评分行 evidence_json 须同构携带口径摘要（F7 规格消费本契约），聚合统一从评分行取权重，不从 dimension 域现读（防当期重算引入新权重）
- LLM 输出的维度 code 有幻觉面，以维度段集合为白名单收敛，缺失补 insufficient 行，禁止直接信任 LLM 枚举
- rationale 是 LLM 从已脱敏档案生成的文本，落库前仍过 Redact 正则集兜底（T4 能力5 的 Redact 函数复用，档案侧已脱敏，此为第二道防线）
- 档案集与 prompt 仅内存临时持有，禁止写日志、缓存与数据库（隐私架构，与 T4 同口径）

#### 能力2: 档案集分层组装

**描述**: evaluator 输入的预算控制。两层结构：

第一层周期统计汇总块：对全部档案（success/failed/skipped 三态）的 Stats 做规则聚合，产出紧凑数字块，无条件全量进 prompt（千字符级，不参与预算判定）。字段：会话总数/有效数（success+failed）/skipped 数、UserMsgCount 合计与零输入会话数、InterruptCount 合计、PasteCharCount 合计与发生粘贴的会话数（PasteCharCount>0 的行数，无消息条次计数字段）、ToolCounts 汇总（top 10）、TurnKindCounts 汇总、SpecFingerprints 汇总（kind 计数 + 去重 hash 数 + 章节标题并集规模）、CmdReuseHashes 去重统计（同首指令命中组数）、continuation 会话数、narrative_absent 会话数、failed 档案数。此块即 C01-C07 承载数据的载体。其中 continuation 会话数取 Stats.ContinuationHit（三态 Stats 均可聚合）；narrative_absent 会话数取 success 行 Behavior 块的 absent 标记计数（T4 契约：absent 落 Behavior 非 Stats，failed 行无 LLM 块天然缺席该计数，与 C06 披露一致）。

第二层逐会话档案块：success 档案的三块文本（Summary/Instruction/Behavior）逐会话组装（每块前缀 session_key 标签），按证据密度降序排列，合计字符数超 MaxProfileSetChars 时截取预算内前 N 个。failed 档案只有统计块（其 Stats 已汇总进第一层），不进第二层。

**证据密度排序**（确定性规则）：density = UserMsgCount×3 + 工具调用总量×1 + SpecFingerprints 数×2 + PasteCharCount>0 ? 2 : 0，降序；同密度按 session_key 字典序稳定排序。设计取向：指令、规约、粘贴是能力维度的主信号，工具量是次信号，密度高的档案优先可见。

**截断披露**：prompt 证据段头部注明「以下为 {VisibleCount}/{TotalSuccess} 个会话档案（按证据密度选取）」，LLM 知悉证据被截取；统计汇总块始终全量，截断只影响逐会话细粒度证据，分级判定的宏观依据不缺失。

**接口定义**:
```go
// AssembleProfileSet 分层组装档案集，纯读不调 LLM 不落库（内部读档案表，需 ctx）。
func (e *Evaluator) AssembleProfileSet(ctx context.Context, tokenName string, period Period) (*ProfileSet, error)
```

**调用示例**:
```go
set, err := ev.AssembleProfileSet(ctx, tokenName, period)
fmt.Printf("visible %d/%d, summary %d chars\n", set.VisibleCount, set.TotalSuccess, len(set.SummaryBlock))
```

**注意事项**:
- 预算判定只统计第二层字符数（三块文本），session_key 标签与分隔符不计（控制结构开销不侵占证据预算）
- 实测参照：最活跃人单周期 143+ 会话、单档案千字符级，全集可达 100k+ 字符，超 90k 预算触发截取属正常路径而非异常；首轮跑批后按 eval_visible_ratio 指标（6.2）观测截取面，若普遍低于 50% 评估下调档案单块规模（T4 模板约束）或上调预算
- skipped 行不进任何层（空壳会话对能力评估无信号价值，其计数已在汇总块的 skipped 数）

#### 能力3: 使用活跃度统计

**描述**: 纯规则统计，不调 LLM。两路输入形态共用同一取数结构：sessions 入参仅承担列表口径计数（SessionCount/TotalTurns）与签名识别的列表判据（tc=1 占比、列表会话量门槛）；档案侧数据（status 分布、client 列、签名档案判据、ValidSessionCount/SkippedCount/ClientDist）两路均内部经 ListByPersonAndRange 读档案表获取（start 前移 7 天缓冲取数，取回后统一按 last_turn_time ∈ [Start, End) 内存过滤归一，口径见步骤3；StatPersonByKey 与 StatPerson 差异仅在列表来源：前者内部经 P2_TECH_002 全量拉取周期窗口会话列表后按 token_name 内存分组，中文 token_name 上游过滤不可用，T4 3.2 既定约束；后者由调用方传入已拉取列表）。统计口径：

1. **窗口归属**：会话按 last_turn_time ∈ [Start, End) 归属本周期（末轮归属规则，边界会话只计一次，会话横跨窗口时前窗口末轮即归属锚点）
2. **列表口径计数**：SessionCount = 窗口内该人会话总数；TotalTurns = turn_count 合计
3. **有效会话数**：ValidSessionCount = 档案集 status 口径全集中 status ∈ {success, failed} 的行数（档案集与能力4 签名识别共用同一集合：ListByPersonAndRange 取回的全部三态行，经 start 前移 7 天缓冲取数后统一按 last_turn_time ∈ [Start, End) 内存过滤，与末轮归属规则及评分侧证据过滤同口径，跨边界会话两侧周期只计一次；skipped 不计入有效；列表口径与档案口径的差值即 skipped_count + 拉取未落行数 + 缓冲窗外被过滤的跨边界行数）
4. **分级判定**：ActiveLevel 按 ValidSessionCount 与维度配置活跃度阈值比较（≥ 活跃下限 → active；≥ 低频下限 → low_freq；否则 unused），阈值运行时读取（默认活跃 10 / 低频 5）
5. **人群签名**：调 IdentifyPopulation（能力4），签名 note 随行落库

落库 activity_stat 行，唯一索引 (token_name, period_start)，重算幂等覆盖（Aggregate 同款 upsert 语义：同人同周期重跑时按唯一索引更新，历史周期行不动）。

**接口定义**:
```go
// StatPersonByKey 拉列表并统计（内部经 P2_TECH_002 拉列表、ListByPersonAndRange 读档案）。
func (a *Activity) StatPersonByKey(ctx context.Context, tokenName string, period Period) (*ActivityStat, error)

// StatPerson 对调用方传入的列表统计（sessions 须已按窗口过滤；
// 档案侧数据仍由内部经 ListByPersonAndRange 读取并透出，供组合入口注入
// Evaluate 免二次取数）。
func (a *Activity) StatPerson(ctx context.Context, sessions []conversationlog.SessionSummary, tokenName string, period Period) (*ActivityStat, []ProfileDigest, error)
```

**调用示例**:
```go
stat, err := act.StatPersonByKey(ctx, tokenName, period)
if err != nil { return err }  // 列表拉取重试耗尽等，交任务重试
slog.Info("activity", "token", tokenName, "level", stat.ActiveLevel,
    "valid", stat.ValidSessionCount, "note", stat.PopulationNote)
```

**注意事项**:
- 分级基于有效会话数而非原始会话数：自动化客户端人群列表口径虚高（C08），经 status 过滤后其 ValidSessionCount 趋零，分级自然落 unused/low_freq，这是活跃度侧承接 C08 的主通道
- 全员循环逐人调 StatPersonByKey 会重复全量拉取列表：T6 编排时应一次拉全量列表内存分组后逐人调 StatPerson（2.5 完整示例）；StatPersonByKey 仅供定向分析等单人场景
- 列表分页须串行翻页（T4 3.2：并发多页返回重复首页数据），本组件按 token_name 分组后按 session_key 去重兜底
- TotalTurns 是列表口径（含 skipped 会话的轮次），表征使用强度；能力证据口径在档案侧，两者语义分离不混用

#### 能力4: 人群签名识别

**描述**: 承接 T4 C08-C12 的规则侧识别，纯函数。输入周期会话列表与档案集，输出人群签名与 evidence_note，只标注不拦截：签名命中只影响活跃度口径修正与评分行的 insufficient 标记，不阻断评估流程。档案集采用 status 口径全集（ListByPersonAndRange 经 start 前移 7 天缓冲取回的全部三态行，统一按 last_turn_time ∈ [Start, End) 内存过滤归一到本周期，与活跃度侧同口径；epoch 首落行经 first_turn_at 窗口过滤自然缺席，属 T4 已披露取舍，见 3.2）：两处调用（能力3 步骤5 与能力1 流程内）共用同一集合口径，与活跃度侧 status 口径对齐，规避同一人活跃度 note 与评分处置按不同档案子集判出矛盾结果；评分侧证据过滤与签名判据同用该过滤后集合，进 LLM 的证据面按证据密度截取，签名判据覆盖全集。

| 签名 | 判定规则（按判定次序设计段定义的优先级匹配，命中即止） | 处置 |
|------|--------------------------|------|
| work_tc1（工作型 tc=1） | tc=1 会话占比 ≥ 50%（分母为列表 SessionCount）且档案 success 占比 ≥ 20%（趋零区分线，非多数口径；分母为档案三态总行数，C09 签名重合区分：先核 status 分布再定性）；success 占比达线但 tc=1 占比未达 50% 时按命中即止落 normal | 正常人群不处置：档案 success 证明单请求内多轮真实工作，不降权；note 落 evidence_note.work_tc1（画像无特殊呈现，仅服务 6.2 按 population_note 分桶观测） |
| bypass_orchestrator（编排工具旁路） | 档案全 skipped 或 success 占比 < 20%（分母同上），且 failed 行占比 < 50%（failed 主导属抽取异常形态，先于签名判定排除，见判定次序设计段），且 client ∈ {workbuddy, omo}，且列表会话量 ≥ 低频下限（C10：真实能力证据在父会话本系统不可见）。client 聚合口径：全部档案行 client 均属旁路族（unknown 与空串视同旁路族并入：探测退化与 detail_invalid 例外近似，T4_004 契约 detail_invalid 行 client 落空串）才判旁路；存在任一正常客户端（claude_code/opencode/mixed）档案行即不判，该行即本系统可见的真实工作证据 | 不强制降级：ActiveLevel 按有效会话数口径自然判（趋零形态下通常落 unused/low_freq），评分行 insufficient；note 记 evidence_note.bypass，画像呈现「数据不可见」而非「零能力」 |
| auto_client（自动化客户端） | 档案全 skipped 或 success 占比 < 20%（分母同上），且 failed 行占比 < 50%（排除条件同 bypass 行），且列表会话量 ≥ 低频下限（列表量正常但档案趋零，C08 签名） | ActiveLevel 强制降为 unused；评分行全维度 insufficient；note 记 evidence_note.auto_client |
| threshold_overskip（阈值误杀） | 档案全 skipped 且列表会话量 ≥ 低频下限且 client 非旁路族（C12：kept 7-9 边缘误杀形态，与旁路同表型；首期该表型按序先落 auto_client，本签名保留待启用） | 同 bypass 落 insufficient；note 记 evidence_note.threshold（首期不生效：判据是 auto_client 子集，按序命中即止下实际落 evidence_note.auto_client，画像无法区分阈值误杀；待首轮跑批回填 client 分布后启用分化判据，届时恢复 threshold 边界标注） |

判定次序设计：四签名共享「档案趋零」表型，区分轴是 success 占比与 client 列。前置排除：failed 行占比 ≥ 50% 的 failed 主导形态属抽取异常（LLM 故障周、上游批量失败），先于签名判定排除，不落任何人群签名（Kind 落 normal、note 空串），其影响经 6.2 eval_fail_ratio 观测、修复走 T4 侧，避免基础设施故障被固化为人群特征标注。边界形态：档案集 0 行（T4 未覆盖该人或列表会话全部未落行，判据分母为零）时，列表会话量 ≥ 低频下限 → 按序落 auto_client 表型（档案趋零极端形态，note 照落），列表会话量为 0 → normal（活跃度自然落 unused）。权威次序为 failed 主导排除 → success 占比 ≥ 20% 走 work_tc1 正常路径 → client ∈ 旁路族判 bypass → 其余档案趋零形态判 auto_client → threshold 签名保留给后续跑批数据区分（首期其判据是 auto_client 判据的子集，按序命中即止下不会命中，仅保留 note 文案与画像语义，待首轮跑批回填 client 分布后再定分化判据）。C11（会议整理子请求稀释）不构成整人签名（散布于正常用户名下，本人另有 success 兜底），识别规则不覆盖，其影响已在档案集稀释中自然消化。

判据中「列表会话量 ≥ 低频下限」的阈值复用自维度配置的低频判定下限（DIM 默认 5），此处口径为列表 SessionCount（与活跃度分级的 ValidSessionCount 口径区分）：列表量门槛用于确认「上游有会话、档案侧趋零」的对比形态，人群签名的门槛判据刻意取列表口径，与 C08 披露的虚高形态对齐。

**接口定义**:
```go
// IdentifyPopulation 人群签名识别，纯函数不调 LLM 不落库。
// sessions 为周期窗口列表（token_name 过滤后），profiles 为对应档案集
// （status 口径全集：ListByPersonAndRange 经 7 天缓冲取回的全部三态行，
// 已统一按 last_turn_time ∈ [Start, End) 过滤归一，见能力4 描述）。
func IdentifyPopulation(sessions []conversationlog.SessionSummary, profiles []ProfileDigest) PopulationSignature

type PopulationSignature struct {
    Kind string // normal / auto_client / bypass_orchestrator / threshold_overskip / work_tc1
    Note string // evidence_note 文案键，空串为 normal
}
```

**调用示例**:
```go
sig := IdentifyPopulation(sessions, profiles)
if sig.Kind != "normal" {
    slog.Info("population signature", "token", tokenName, "kind", sig.Kind)
}
```

**注意事项**:
- 签名判定是启发式规则，误判代价是 note 标注偏差（画像呈现口径），不阻断任何评估路径；首轮跑批后按 6.2 population_signature_ratio 观测各签名占比，偏离预期时调判定阈值
- note 文案键经 i18n 前端呈现（F6/F9 消费），本组件只落枚举键不落中文文案，避免文案变更动数据
- client 列依赖 T4 分层探测（P2_TECH_004）落库，探测 unknown 时 bypass 判定退化为 auto_client 表型并列（可接受近似，观测侧按 client_dist_json 人工核对）

#### 能力5: 多维打分与聚合

**描述**: 维度分到模块分到总览分的规则聚合，不调 LLM。聚合输入是评分行全量（conversation 与 active_test 两 source 并列，主动测试维度由 F7 阅卷写入，本组件统一聚合）。聚合的权重与 InOverview 门槛一律取评分行 evidence_json 的口径摘要（快照权重，含 active_test 行，F7 写入时落快照），不从 dimension 域现读，保证配置变更后同人同周期重算口径稳定：

1. **聚合域**：同人同周期全部 status=success 评分行（周期匹配按 period_start 与 period_end 均精确等于传入 period，定向分析窄窗口行因 period_start 不同天然隔离）；insufficient 维度与 failed 维度（无分）剔除出聚合
2. **模块分**：模块内参与总览（InOverview）且非剔除的维度，按权重加权平均，保留一位小数；模块内全部剔除时该模块分置 nil（画像呈现「数据不足」）
3. **总览分**：全部参与维度的加权平均（跨模块直接聚合，权重即各维度权重百分比），保留一位小数；无任何参与维度时置 nil
4. **权重归一化**：剔除 insufficient 维度后剩余权重和为 W'，模块分/总览分按 W' 归一化（等价于剔除维度权重清零后重分配），口径见 2.3 AggregateResult 的 included/excluded 快照
5. **幂等重算**：Aggregate 按唯一索引 upsert（同人同周期同模块覆盖更新），F7 阅卷新增评分行后重调即刷新聚合；历史周期行不动（配置变更历史不重算）
6. **failed 行与空聚合覆盖**：failed 评分行（无分）一律剔除出聚合且不阻断重算——同人同周期重算时聚合行照常覆盖落库，全剔除时模块分/总览分 nil 显式落空（与 5.1「全剔除总览」用例同口径），旧聚合行的非空值不保留；画像侧按数据不足呈现，补跑重评后重调即恢复

**聚合公式**（模块分，总览分同构）：

```
module_score = Σ(score_i × weight_i) / Σ(weight_i)，i ∈ 参与维度
```

**接口定义**:
```go
// Aggregate 重算单人周期的聚合行（读全部 source 评分行），幂等。
func (s *Scorer) Aggregate(ctx context.Context, tokenName string, period Period) (*AggregateResult, error)
```

**调用示例**:
```go
agg, err := sc.Aggregate(ctx, tokenName, period)
if err != nil { return err }
slog.Info("aggregate", "token", tokenName, "overview", *agg.Overview,
    "excluded", len(agg.Excluded["ai_usage"]))
```

**注意事项**:
- 权重和偏离 100 是配置侧常态（DIM 规则只建议不强制）：聚合按实际权重归一化，不校验不纠偏，画像侧呈现权重快照即可解释
- 九型人格等参考性维度（InOverview=false）不进聚合，只落评分行供画像补充解读
- 聚合与评分解耦：F7 阅卷链路写入评分行后自调 Aggregate 刷新，本组件不感知阅卷时机（T6/F7 编排）
- float 落库精度：模块分/总览分保留一位小数（round half up），跨库 float 存储用 float64 列（GORM 通用类型）

#### 能力6: 单人周期评估原子入口

**描述**: 供 T6 编排的一人一次组合入口。执行序列：活跃度统计（StatPersonByKey）→ 综合评估（Evaluate，含档案取数、签名识别、组装、LLM、评分落库）→ 聚合（Aggregate）→ 返回组合结果。幂等规则：

1. 该人该周期已有全部 success 评分行（source=conversation 的对话分析维度数齐全且无 failed）时，跳过 LLM 调用直接复用（Reused=true），活跃度与聚合照常重算（列表数据与 F7 评分行可能已变）；幂等判定只看 conversation 行，active_test 行不参与判定；零 LLM 跳过路径落的 insufficient success 行带 skip_no_llm 标记（error_code 列），不进复用判定而走先删后评（评估任务先于抽取落库运行时，档案到位后重跑可收敛，自愈通道）。判定与复用逻辑内聚在 Evaluate 内，EvaluatePerson 仅做组合编排
2. 存在 failed 评分行或 skip_no_llm 标记行时重评：删旧行（仅 source=conversation 的行，删除由 Evaluate 内部执行）后走完整 LLM 调用（failed 翻转语义与 T4 档案行同构，但评分行是整体重评非单维补评，LLM 一次调用产出全维度），F7 阅卷写入的 active_test 行不受删除影响
3. 周期批量与手动定向分析复用本入口：定向分析传入更窄 period，落库行按 (token_name, period_start) 隔离，与周期批量行并存；唯一索引不含 period_end，定向窗口起点与批量窗口起点重合时按幂等覆盖语义处理（定向行覆盖批量行，批量窗口评分被替换），定向分析入口（F6）须提示运营避开批量窗口起点选时段

周期批量时 T6 先一次拉全量列表分组，逐人调 EvaluatePerson 前把分组列表经 StatPerson 注入（2.5 完整示例）；本入口的 StatPersonByKey 路径仅供单人场景，避免全员循环重复拉取。

**接口定义**:
```go
// EvaluatePerson 单人周期评估原子入口（活跃度 → 评估 → 聚合）。
// sessions 可空：空时内部 StatPersonByKey 拉取；非空时直传 StatPerson 免二次拉取。
func (e *Evaluator) EvaluatePerson(ctx context.Context, tokenName string, period Period, sessions []conversationlog.SessionSummary) (*EvaluateResult, error)
```

**调用示例**:
```go
res, err := ev.EvaluatePerson(ctx, tokenName, period, nil)
if err != nil { return err }  // 交 Asynq 任务级重试
if res.Reused { slog.Info("eval reused", "token", tokenName) }
```

**注意事项**:
- 组合入口不包事务：三表落库各自独立（活跃度行、评分行、聚合行），中途失败重跑时已落库行按幂等规则收敛，不回滚（评估数据可重算，无需跨表原子性）
- LLM 段失败（ErrLLMEvalUpstream/ErrSchemaInvalid）落 failed 评分行后组合入口继续执行聚合（聚合自然剔除 failed 维度），当期推进优先，补跑时重评；活跃度行不受 LLM 失败影响照常落库
- error 通道只留给基础设施类错误（配置读取失败、落库失败、列表拉取重试耗尽 ErrSessionListFetch），交 Asynq 重试；业务态（零档案跳过、LLM 降级）不占 error 通道，与 T4 错误分流哲学一致

### 2.5 使用示例

#### 基础用法

```go
// 定向分析场景：运营选定某人与某时段，单人评估
ev := evaluator.New(llmClient, clClient, sessionFeatureRepo, dimensionReader,
    scoreRepo, aggregateRepo, activityRepo)
res, err := ev.EvaluatePerson(ctx, "李雪涛", Period{Start: start.Unix(), End: end.Unix()}, nil)
if err != nil {
    return err // Asynq 重试
}
if res.Skipped {
    // 零有效档案：活跃度行 + 全维度 insufficient 评分行已落库，画像呈现数据不足
    return nil
}
```

#### 高级用法

```go
// 调试或回归：只看档案集组装效果（预算、截取、密度排序），不调 LLM 不落库
set, err := ev.AssembleProfileSet(ctx, tokenName, period)
if err != nil { return err }
fmt.Printf("summary block: %d chars\nvisible: %d/%d sessions\n",
    len(set.SummaryBlock), set.VisibleCount, set.TotalSuccess)
```

#### 完整示例

```go
// 周期跑批主循环（T6 pipeline 编排）：一次拉全量列表内存分组，逐人评估
items, total, err := cl.ListSessions(ctx, secret, conversationlog.ListSessionsRequest{
    StartTime: period.Start, EndTime: period.End, Page: 1, PageSize: 100,
})
if err != nil { return err }
// …串行翻页拉全（并发翻页返回重复页，T4 3.2）…
byPerson := map[string][]conversationlog.SessionSummary{}
for _, it := range items {
    byPerson[it.TokenName] = append(byPerson[it.TokenName], it)
}
for tokenName, sessions := range byPerson {
    res, err := ev.EvaluatePerson(ctx, tokenName, period, sessions) // 复用分组列表
    if err != nil {
        slog.Error("person eval failed", "token", tokenName, "err", err)
        continue // 单人失败不阻断全员循环，交 Asynq 重试
    }
    _ = res // 活跃度行、评分行、聚合行均已落库，画像与看板经 repository 消费
}
```

---

## 3. 技术约束

### 3.1 性能要求

| 指标 | 要求 | 说明 |
|------|------|------|
| 活跃度统计耗时 | ≤ 500ms / 人（不含列表拉取网络耗时） | 纯内存规则统计（分组、去重、status join、阈值比较） |
| 档案集组装耗时 | ≤ 200ms / 人（150 档案内） | 全量 Stats 聚合 + 密度排序 + 截取，内存操作 |
| 综合评估 LLM 调用 | ≤ 120s / 人（单次调用口径，正常路径） | 受 LLM 响应主导；底座重试与最坏路径由 2.2 的分路径 ctx 预算承载（评估专用 client Timeout 180s，接口设计 §2.1） |
| 聚合耗时 | ≤ 100ms / 人（20 维度内） | 评分行读取 + 加权平均 + upsert，线性复杂度 |
| 档案集预算 | 逐会话块合计 ≤ MaxProfileSetChars（90000 字符，30000 token 折算） | 统计汇总块必进不占预算；截取按证据密度，超限属正常路径（1.3 实证） |
| 并发吞吐 | 由 worker 并发度承载，评估通道 LLM 在飞 ≤ 4（评估专用 client 独立 gate，跨通道总在飞为各通道之和） | 一人一任务；评估调用是周期跑批的吞吐瓶颈，成本按周频核算 |
| 内存占用 | 档案集与 prompt 会话级临时持有 | 处理完成即释放，无跨人缓存 |

### 3.2 兼容性要求

#### 版本兼容性

| 依赖 | 版本要求 |
|------|---------|
| Go | 1.25（hr-backend module sili-smart-hr/backend） |
| P2_TECH_003/004 会话特征抽取 | session_features 表契约（profile_json 四块结构、status 三态、client 列、ListByPersonAndRange 取数） |
| P2_TECH_001 LLM 调用底座 | 统一调用入口（排他启用唯一模型，OpenAI 兼容与 Anthropic 双协议、并发 4） |
| P2_TECH_002 会话日志集成 | SessionSummary 契约（列表拉取、Bearer 鉴权、错误分类） |
| P2_DIM_001 维度与权重配置 | dimension 域读取契约（启用维度、提示词、锚点、权重、活跃度阈值） |
| SQLite / PostgreSQL / MySQL | 三库兼容：三张新表 tag 用 GORM 通用类型，JSON 存 TEXT 列，时间 time.Time，布尔不加 default tag |

#### 上游数据兼容

| 上游形态 | 兼容口径 |
|---------|---------|
| 中文 token_name 上游过滤不可用 | 列表全量拉取后按 token_name 内存分组（T4 既定约束），禁止依赖上游按人过滤参数 |
| 列表分页并发重复页 | 串行翻页；分组后按 session_key 去重兜底（T4 3.2） |
| 档案表时间列 epoch 形态 | T4 披露的 ErrDetailFetch 首落行 turn_count=0、时间列 epoch：ListByPersonAndRange 按 first_turn_at 窗口过滤（上游契约），epoch 行（1970-01-01）恒落窗外进不了两侧任何结果集，评分侧（last_turn_time 二次过滤）与活跃度侧（status 口径计数）均无 epoch 行参与。其有效会话漏计与人群签名判据缺席属 T4 已披露取舍（epoch 行对窗口查询不可见，影响限于该行自身），密钥修复后补跑翻转即恢复可见 |
| 跨窗口边界会话 | 末轮归属规则（能力3 列表侧 + 能力1/3/4 档案侧）：last_turn_time 归属单一周期。列表侧上游时间过滤同口径（T3 既定事实表）；档案侧 ListByPersonAndRange 按上游契约以 first_turn_at 过滤，评分侧与活跃度/签名侧取数时均 start 前移 7 天缓冲后统一按 last_turn_time ∈ [Start, End) 内存过滤归一，两侧口径一致，漏计面仅限缓冲窗外的超长跨边界会话（first_turn 早于 Start-7d 而 last_turn 落本周期，跨周以上长会话），跨边界会话跨周期只计一次无双计 |
| 档案 profile_json 结构演进 | T4 模板版本化演进（prompt 模板约束变更）后历史档案与新档案混存：解析宽容处理（未知字段忽略、缺失字段零值），评分 prompt_version 落库标记口径代差，观测侧可按版本拆分 |
| 维度配置缺失 | ErrNoDimensions 阻断评估属配置前置问题；部分维度缺提示词时（DIM 规则6：对话分析维度必填提示词，未填无法评分）该维度按 insufficient 处理，rationale 落配置缺省文案并在 evidence_json 口径摘要记 config_missing 标记，不阻断其余维度 |

### 3.3 安全要求

| 安全项 | 要求 | 实现方式 |
|--------|------|---------|
| 对话原文不进本组件 | 本组件只消费脱敏档案，不接触原文 | 档案自 session_features 表读取（T4 已脱敏），无原文通道；档案集与 prompt 仅内存临时持有，禁止写日志、缓存与数据库 |
| 评分理由脱敏 | rationale 落库前过 Redact 正则集 | T4 Redact 函数复用（第二道防线：档案侧已脱敏，LLM 复述仍可能拼出敏感形态） |
| 人名不进 LLM 上下文 | prompt 组装前剥离 TokenName | 人员归属落库时回填 token_name 列 |
| 日志最小化 | 日志只含 token_name、period、计数、维度 code、错误码 | 禁止输出档案内容、prompt 文本、rationale 原文（rationale 落库供画像消费，不进日志） |
| 人员标识最小化 | 三表只存 token_name 归属字段 | 无工号（系统无工号约束），无 user_id 冗余（全库恒 1 无区分度） |
| 评分可追溯 | 每维度评分行携带 evidence_json | 支撑证据的 session_key 清单、统计摘要快照与维度口径摘要（weight/InOverview，聚合唯一权重来源，规则产出非 LLM 引用），服务 PRD 评分可解释诉求 |

---

## 4. 集成方式

### 4.1 安装方式

后端进程内组件，随 hr-backend 单体部署，无独立安装。代码落位：

```
hr-backend/internal/engine/
├── evaluator/          # 能力1/2/6：跨会话综合评估、档案集组装、原子入口（替换 doc.go 占位）
├── scorer/             # 能力5：多维打分与聚合（替换 doc.go 占位）
└── activity/           # 能力3/4：活跃度统计与人群签名识别（替换 doc.go 占位）
```

人群签名识别（IdentifyPopulation）为纯函数，落 activity 包（消费主体）。

### 4.2 引入方式

```go
import "sili-smart-hr/backend/internal/engine/evaluator"

// Wire 装配：config → repository（SessionFeature 既有 + 三张新表）→
// evaluator（依赖 llm 客户端、conversationlog 客户端、dimension 读取、三表 repository）
ev := evaluator.New(llmClient, clClient, sessionFeatureRepo, dimensionReader,
    scoreRepo, aggregateRepo, activityRepo)

// worker 任务注册（worker/task 内，T6 编排注册，本组件提供处理器函数）：
// const TypePersonEvaluate = "engine:person-evaluate"
// payload: {token_name, period_start, period_end}
```

评分与聚合表 repository 随本组件新建（dimension_score / aggregate_score / activity_stat），DDL 细节归 04 接口数据设计技能；evidence_json、included_json、excluded_json、client_dist_json 均 TEXT 列存序列化 JSON（多库兼容约定）。

### 4.3 依赖项

#### 必需依赖

| 依赖 | 版本 | 用途 |
|------|------|------|
| P2_TECH_003/004 会话特征抽取 | 已实现 | 特征档案唯一来源（ListByPersonAndRange 三态行、client 列、Redact 函数复用） |
| P2_TECH_001 LLM 调用底座 | 已实现 | 综合评估唯一 LLM 通道（并发 4、token 预算、重试由底座承载） |
| P2_TECH_002 会话日志集成 | 已实现 | 活跃度统计的会话列表拉取通道 |
| P2_DIM_001 维度与权重配置 | 已实现 | 评分口径（提示词/锚点/权重）与活跃度阈值的配置来源 |
| GORM（三张新表） | v2 | dimension_score / aggregate_score / activity_stat 落库 |
| Asynq worker | 已实现 | 一人一任务的调度承载（T6 编排） |

#### 可选依赖

| 依赖 | 版本 | 用途 |
|------|------|------|
| pipeline 子域（T6） | 未实现 | 跑批编排消费方：周期调度、逐人任务分发、失败重试、失败比例聚合由 T6 承载 |
| F6 评估运营 / F7 主动测试阅卷 | 未实现 | F7 阅卷写入 active_test 评分行后调 Aggregate 刷新聚合；F6 消费三表数据生成画像快照 |

## 5. 测试方案

### 5.1 单元测试

| 测试场景 | 测试方法 | 预期结果 |
|---------|---------|---------|
| 窗口归属与去重 | 构造横跨窗口边界的会话（first_turn 在前周期、last_turn 在本周期）与重复 session_key 列表，两侧分别断言：列表侧走 StatPerson、档案侧走 Evaluate 捕获进 prompt 的 session_key 集合 | 列表与档案均只归属 last_turn_time 所在周期（档案取数含 7 天缓冲，缓冲窗内 last_turn 在前周期的行被内存过滤排除）；重复键去重后计数正确 |
| 活跃度分级 | 预置 ValidSessionCount 分别为 12（≥活跃下限 10）、7（低频区间）、3（低于低频下限）的档案集 | 分别判 active / low_freq / unused；阈值从 dimension 读取生效（改阈值重判翻转） |
| 有效会话数口径 | 构造 success 10 + failed 2 + skipped 30 的档案集 | ValidSessionCount=12（skipped 不计有效），SessionCount=42，skipped_count=30 |
| 活跃度幂等 | 同人同周期重跑 StatPerson | 唯一索引 upsert，行数不增，字段更新 |
| auto_client 签名 | 构造列表会话量 20、档案全 skipped、client=claude_code 的输入（自动化经正常客户端表型，存在正常客户端行不判旁路，按序落 auto_client） | 签名 auto_client，ActiveLevel 强制 unused，note 落 evidence_note.auto_client |
| bypass 签名 | 构造档案全 skipped、client=workbuddy、列表量正常的输入 | 签名 bypass_orchestrator，不强制降级字段（ActiveLevel 自然落 unused），note 落 evidence_note.bypass |
| work_tc1 正常路径 | 构造 tc=1 占比六成、档案多为 success 的输入（C09 工作型） | 签名 work_tc1（tc=1 占比 ≥ 50% 且 success 占比 ≥ 20%），不降权，note 落 evidence_note.work_tc1，评估照常走 LLM |
| tc=1 未达线落 normal | 构造 tc=1 占比 30%、档案多为 success 的输入 | success 占比达线但 tc=1 占比未达 50%，按命中即止落 normal（note 空串） |
| failed 主导排除 | 构造 failed 行占比 80%（success=0）且列表量正常的输入 | 先于签名判定排除，Kind 落 normal、note 空串，ActiveLevel 按有效会话数正常判，不落 auto_client/bypass |
| 签名判定次序 | 构造 success 占比 25% 且 client=workbuddy 的混合输入 | success 占比 ≥ 20% 先命中工作型路径，不落旁路签名 |
| 混合 client 不判旁路 | 构造档案全 skipped、client 混合（部分 workbuddy + 部分 claude_code）的输入 | 存在正常客户端档案行即不判 bypass_orchestrator，按序落 auto_client 表型 |
| 空串 client 并入旁路族 | 构造档案全 skipped、client 全为空串（detail_invalid 形态）且列表量正常的输入 | 空串视同旁路族并入，与 client=workbuddy 同判 bypass_orchestrator 表型 |
| 空档案集边界 | 分别构造档案 0 行且列表量 ≥ 低频下限、档案 0 行且列表量为 0 的输入 | 前者按序落 auto_client（档案趋零极端形态，note 照落），后者落 normal（活跃度自然落 unused），不因分母为零产生未定义行为 |
| 汇总块聚合 | 预置三态档案集（含零输入会话、narrative_absent、failed、CmdReuseHashes 同首命中） | SummaryBlock 各字段计数正确（UserMsgCount 合计、粘贴合计、ToolCounts top10、指纹去重、continuation/absent 计数） |
| 密度排序与截取 | 构造 200 个 success 档案（合计超 MaxProfileSetChars），密度各异 | 按密度降序截取预算内前 N，VisibleCount/TotalSuccess 披露正确；同密度按 session_key 字典序稳定 |
| 预算不触发路径 | 构造 10 个档案（合计远低预算） | 全量进视图无截取，VisibleCount == TotalSuccess |
| LLM fake 正常评分 | fake LLM 返回合法 dimensions JSON（含 insufficient 维度） | 评分行落库：score 正确、insufficient 维度 score=0 且标记、rationale 落库、evidence_json 含 session_key 清单 |
| LLM code 幻觉收敛 | fake LLM 返回含未知 code、缺一维度、score 越界（-5/105）的输出 | 未知 code 丢弃；缺失维度补 insufficient 行；越界 score 判 schema 失败走重试 |
| schema 宽容解析 | fake LLM 分别返回 markdown 围栏包裹、JSON 后跟解释文字的输出 | 剥围栏、截平衡对象后解析成功，不误判 schema 失败 |
| rationale 超长 | fake LLM 返回 500 字 rationale（超 2×200 容差） | 判 schema 失败重试一次，仍超长落 failed 评分行 |
| rationale 兜底脱敏 | fake LLM 返回含 sk- 密钥与 Windows 路径的 rationale | 落库行 rationale 已替换 [SECRET]/[PATH] 占位符 |
| 零有效档案跳过 | 预置全 skipped 档案（或空档案集）走 Evaluate | Skipped=true 且 err=nil，零 LLM 调用（fake 断言），全维度 insufficient 评分行落库 |
| 维度配置缺失 | dimension 读取返回空启用维度集 | ErrNoDimensions 上抛，不落评分行 |
| 部分维度缺提示词 | 构造 1 个对话分析维度提示词为空 | 该维度落 insufficient 行并披露，其余维度照常评分 |
| TokenName 剥离 | 捕获喂 LLM 的完整 prompt | prompt 全文无 token_name 人名 |
| 评分幂等复用 | 预置全 success 评分行重跑 EvaluatePerson | Reused=true，fake LLM 断言零调用，活跃度与聚合照常重算 |
| failed 评分行重评 | 预置 failed 评分行，fake LLM 恢复后重跑 | 旧 failed 行删除，新行落库，无重复行 |
| 聚合加权与剔除 | 预置 3 维评分（80/60/40，权重 50/30/20）其中 1 维 insufficient | 模块分 = (80×50+40×20)/70 ≈ 68.6；Excluded 含剔除维度；included_json 落维度编码与权重快照 |
| 全剔除模块 | 预置某模块全部维度 insufficient | 该模块分 nil，总览分按其余模块参与维度计算 |
| 全剔除总览 | 预置全部维度 insufficient | Overview nil，模块分全 nil，聚合行照常落库（nil 显式落空） |
| 跨 source 聚合 | 预置 conversation 3 维 + active_test 2 维评分行 | 聚合统一纳入（AI 管理能力模块分出现），幂等重算刷新 |
| 权重偏离 100 | 预置权重合计 80 的参与维度 | 照常按实际权重归一化聚合，不报错 |
| float 精度 | 构造除不尽的加权平均（如 68.571…） | 落库保留一位小数（68.6），round half up |
| 聚合幂等 | 同人同周期重跑 Aggregate | upsert 覆盖更新，行数不增 |
| 组合入口序列 | fake 全链路跑 EvaluatePerson | 三表落库齐备（activity_stat + dimension_score + aggregate_score），执行顺序活跃度→评估→聚合 |
| LLM 失败不阻断聚合 | fake LLM 持续失败跑 EvaluatePerson | 评分行落 failed，聚合照常执行（failed 维度剔除），活跃度行照常落库，err=nil |
| ctx 预算 | 构造 900s 预算 ctx 与 fake LLM 延迟 | 底座超时语义透传，取消立即中止不残留半写状态（落库原子性由 upsert 承载） |

### 5.2 集成测试

| 测试场景 | 测试步骤 | 预期结果 |
|---------|---------|---------|
| 单人全链路 | fake conversationlog 列表 + 预置档案表数据 + fake LLM + SQLite 内存库，走 EvaluatePerson | 三表出现完整行，token_name 归属正确，日志无 prompt 与 rationale 内容 |
| 周期批量形态 | 预置多人多窗口数据，模拟 T6 主循环（一次拉列表分组逐人评估） | 各人三表行独立落库，单人 LLM 失败不阻断他人 |
| 定向分析与周期并存 | 先跑周期批量，再对同人跑窄窗口定向分析 | 两周期行按 period_start 隔离并存，互不覆盖；定向窗口起点与批量起点重合时按幂等覆盖语义处理（行被覆盖，符合能力6 规则3 披露） |
| Evaluate 幂等周期匹配 | 预置同人同 period_start、不同 period_end 的 success 评分行，重跑 Evaluate 传入窄 period | 幂等判定按双界精确匹配，不命中宽窗口行，正常走 LLM 后按唯一索引覆盖落库（覆盖语义成立，非 Reused 跳过） |
| 活跃度阈值联动 | 维度配置页改活跃度阈值后重跑 StatPerson | 新阈值生效，分级按新口径翻转 |
| F7 阅卷后聚合刷新 | 预置 conversation 评分行，模拟 F7 写入 active_test 评分行后调 Aggregate | 聚合行刷新纳入新维度，模块分变化可追溯（included 快照） |
| 与 T4 档案表联动 | 预置含 failed（ErrDetailFetch epoch 行）与 skipped 行的档案集 | epoch 时间行经窗口过滤自然排除，status 口径计数正确（C06/T4 披露口径承接） |

### 5.3 性能测试

| 测试项 | 测试方法 | 验收标准 |
|--------|---------|---------|
| 活跃度统计性能 | 150 档案 + 对应分组列表跑 StatPerson | ≤ 500ms |
| 汇总块与组装性能 | 150 档案（含 100k+ 字符三块文本）跑 AssembleProfileSet | ≤ 200ms |
| 密度排序稳定性 | 200 档案随机密度重复组装 | 排序结果确定（同输入同输出），无重复无遗漏 |
| 聚合性能 | 20 维度评分行跑 Aggregate | ≤ 100ms |
| 档案集预算控制 | 200 档案跑组装断言视图字符数 | 逐会话块合计 ≤ 90000 字符，截取披露正确 |

### 5.4 测试覆盖率要求

- 单元测试覆盖率: ≥ **80%**
- 核心能力（活跃度统计、人群签名、档案集组装、schema 校验收敛、聚合剔除与归一化、幂等）覆盖率: ≥ **95%**

---

## 6. 监控与运维

### 6.1 日志规范

| 日志级别 | 使用场景 | 示例 |
|---------|---------|------|
| DEBUG | 组装统计、单次评估成功 | profile set assembled token=... visible=45/143 summary_chars=2100 |
| INFO | 人群签名命中、零档案跳过、评分复用 | population signature token=... kind=auto_client / eval reused token=... |
| WARN | schema 校验失败重试、档案集截取触发 | eval schema invalid, retrying token=... / profile set truncated token=... visible=45/143 |
| ERROR | LLM 评估失败、落库失败、配置读取失败 | eval failed token=... code=ErrLLMEvalUpstream |

日志字段只含 token_name、period、维度 code、计数与错误码，禁止输出档案内容、prompt 文本、rationale 原文（隐私架构）。

### 6.2 监控指标

| 指标名称 | 指标类型 | 说明 | 告警阈值 |
|---------|---------|------|---------|
| eval_fail_ratio | 比率 | 单周期综合评估失败率（failed 评分行人次 / 评估人次）。口径披露：ErrStoreWrite 落库失败的人次无评分行，漏计属已知口径，ERROR 日志计数辅助观测（与 T4 extract_fail_ratio 同构）。10% 为出厂默认阈值，最终取值以系统参数页配置为准（PRD 4.4 约定，参数契约归 T6/F11） | ≥ 10% 触发工作台待处理告警（出厂默认，对齐架构 2.2 约定） |
| eval_schema_fail_ratio | 比率 | schema 校验失败占 LLM 调用比 | ≥ 20% 观测（模型 JSON 能力或 prompt 退化信号，参照 T4 问题3） |
| eval_visible_ratio | 比率 | 档案集截取面：VisibleCount/TotalSuccess 的人均加权均值 | 人均值 < 50% 连续两周期观测（评估调整 T4 单档案规模或本预算，见 2.4 能力2 注意事项） |
| population_signature_ratio | 比率 | 各人群签名占评估人次比（按 activity_stat.population_note 文案键分桶，work_tc1 经独立 note 键分桶可见，normal 空串桶不计入指标） | 无告警，仅观测（首轮跑批回填基线，偏离预期时调判定阈值） |
| unused_ratio | 比率 | 未使用人群占比（ActiveLevel=unused） | 无告警，仅观测（对齐 PRD 未使用人群识别功能的数据基础） |
| eval_duration | 延迟 | 单人综合评估耗时分布（含 LLM） | p99 > 180s 观测（正常路径 LLM 主导 ≤ 120s） |
| aggregate_duration | 延迟 | 单人聚合耗时分布 | p99 > 1s 观测 |

### 6.3 告警规则

| 告警名称 | 触发条件 | 严重程度 | 处理建议 |
|---------|---------|---------|---------|
| 评估失败率超阈 | 单周期 eval_fail_ratio ≥ 10% | 中 | 运营经工作台决定是否补跑（复用手动定向分析入口），补跑走 failed 评分行重评 |
| schema 失败集中 | eval_schema_fail_ratio ≥ 20% | 中 | 核对当前排他启用模型与大模型配置，必要时切换模型后补跑；检查 prompt 模板版本 |
| 档案集截取面过大 | eval_visible_ratio 人均值 < 50% 连续两周期 | 低 | 评估下调 T4 档案单块规模（走 T4 模板变更）或上调 MaxProfileSetTokens（代码变更），二者取一 |

### 6.4 故障排查

#### 常见问题

**问题1: 大批人员评分全 insufficient**
- 现象: 某周期大量人员全维度 insufficient，population_signature_ratio 中 auto_client/bypass 占比抬升
- 原因: 上游注入形态变更致档案大量落 skipped（T4 黑名单失效连锁），或真实人群变化（新自动化工具接入）
- 解决: 先查 T4 的 skip_ratio 与 inject_residual_ratio（按 client 拆分），确认档案侧根因；本组件签名判定只是下游呈现，修复在 T4 参数页或规则层

**问题2: 评分理由质量差或维度分异常**
- 现象: rationale 空泛、分数集中某档、跨人无区分度
- 原因: 档案集截取面过大（eval_visible_ratio 低，证据不足）、维度提示词或锚点配置质量问题、模型能力不足
- 解决: 按序排查：eval_visible_ratio 确认证据面 → 维度配置页核对提示词与锚点 → 大模型配置页核对启用模型；prompt 模板调整走代码变更

**问题3: 聚合分与画像呈现不一致**
- 现象: 模块分/总览分与画像页呈现对不上
- 原因: F7 阅卷写入后未触发 Aggregate 刷新（编排遗漏），或权重配置变更后历史行未重算（属设计内：配置下次跑批生效）
- 解决: 核对 included/excluded 快照定位口径差异；编排侧确认阅卷后刷新调用；配置变更等下周期自然收敛

**问题4: 活跃度分级与直觉不符**
- 现象: 会话量很大的人被判 unused/low_freq
- 原因: 有效会话数口径（skipped 不计），该人多为自动化客户端或旁路会话（签名 note 可证）
- 解决: 查 population_note 与 client_dist_json 确认人群归类；属设计内口径（C08 承接），如需人工复核按 client 分布核对

---

## 7. 附录

### 7.1 术语表

| 术语 | 说明 |
|------|------|
| 跨会话综合评估 | 读单人周期内全部特征档案一次性调 LLM 评完全部对话分析维度（每人周期一次，成本收敛口径） |
| 维度评分行（dimension_score） | 单维度单周期的评分记录：分数、理由、insufficient 标记、证据清单、口径快照 |
| 聚合行（aggregate_score） | 单模块单周期的聚合记录：模块分、总览分、参与/剔除维度快照 |
| 活跃度行（activity_stat） | 单人单周期的活跃度记录：会话计数、有效会话数、分级、人群签名标注 |
| 有效会话数（ValidSessionCount） | 档案表 status ∈ {success, failed} 的会话数，skipped 不计（活跃度分级口径） |
| 有效档案 | 档案表 status=success 的档案行（评分侧口径，MinValidProfilesForEval 判据）；与有效会话数的差异在 failed 态：failed 行计有效会话数但不算有效档案（其证据缺失触发降权） |
| 末轮归属 | 会话按 last_turn_time 归属评估周期的窗口对齐规则（跨边界会话只计一次） |
| 证据密度（density） | 档案证据强度的确定性评分（指令×3 + 工具×1 + 指纹×2 + 粘贴 +2），档案集截取排序依据 |
| insufficient / 数据不足降权 | 维度证据不足标记：评分照出但聚合时剔除（等价权重归零），画像呈现维度分加标注 |
| 人群签名（population signature） | 按列表口径与档案 status 分布识别的人群分类（auto_client/bypass_orchestrator/threshold_overskip/work_tc1），只标注不拦截 |
| 档案集分层组装 | evaluator 输入的两层结构：周期统计汇总块（全量必进）+ 逐会话档案块（按密度截取） |
| 剔除归一化 | 聚合时 insufficient 维度权重清零后剩余权重重新归一（等价于剔除维度重分配权重） |
| 评分口径快照 | 评分行落库时携带的维度配置（提示词/锚点/权重）与 prompt 版本，保证历史评分可追溯 |

### 7.2 T4 消费侧约束承接表

T4（P2_TECH_003/004）附录 7.2 的 C01-C13 约束在本组件的承接位置：

| T4 约束 | 承接位置 |
|---------|---------|
| C01 零输入会话指令稀薄 | 2.2 评分 prompt 组装规则表 + 能力2 汇总块字段（UserMsgCount 合计、零输入会话数） |
| C02 narrative_absent 降权 | 2.2 组装规则表 + 能力2 汇总块（absent 会话数占比） |
| C03 工具证据粒度 | 2.2 组装规则表（prompt 披露） |
| C04 同首指令按重做 | 2.2 组装规则表 + 能力2 汇总块（CmdReuseHashes 去重统计） |
| C05 粘贴量联合判读 | 2.2 组装规则表 + 能力2 汇总块（三量联合呈现） |
| C06 failed 行参与降权 | 能力1（档案二次过滤）+ 汇总块 failed 计数 + 3.2 兼容口径（epoch 行处理） |
| C07 续接回顾文本双重计数 | 2.2 组装规则表 + 汇总块 continuation 计数 |
| C08 自动化客户端活跃度虚高 | 能力3 有效会话数口径 + 能力4 auto_client 签名（强制 unused） |
| C09 tc=1 签名重合区分 | 能力4 work_tc1 判定（success 占比 ≥ 20% 先核档案再定性） |
| C10 编排旁路族群 | 能力4 bypass_orchestrator 签名（client 列判据，「数据不可见」非「零能力」） |
| C11 会议整理子请求稀释 | 能力4 注意事项（不构成整人签名，档案集稀释自然消化） |
| C12 kept 7-9 阈值误杀 | 能力4 threshold_overskip 签名（首期部分承接：判据子集关系致首期不生效，形态归 auto_client note 呈现，数据不足降权照常承接；阈值边界标注待跑批数据回填后启用） |
| C13 skip_ratio 按人群拆分 | 6.2 population_signature_ratio 观测指标（首轮跑批回填基线） |

### 7.3 参考文档

- [PRD](../../02_prd/PRD.md)（5.2 Feature 清单 T5 综合评估与聚合；4.1 对话分析评估；4.4 异常处置）
- [架构文档](../../03_architecture/architecture.md)（2.2 引擎底座六子域分工、3.1.2 LLM 集成成本控制与并发约定、4.6 数据库约定）
- [P2_TECH_003 会话特征抽取规格](../P2_TECH_003_TECH_会话特征抽取/01_功能需求规格说明书.md)（档案契约、C01-C13 消费侧约束清单、Redact 复用）
- [P2_TECH_004 抽取规则分层规格](../P2_TECH_004_TECH_抽取规则分层/01_功能需求规格说明书.md)（client 列契约、人群签名识别维度）
- [P2_TECH_002 会话日志集成规格](../P2_TECH_002_TECH_会话日志集成/01_功能需求规格说明书.md)（SessionSummary 契约、列表拉取与错误分类）
- [P2_TECH_001 LLM 调用底座规格](../P2_TECH_001_TECH_LLM调用底座/01_功能需求规格说明书.md)（统一调用入口、并发 4、token 预算）
- [P2_DIM_001 维度与权重配置规格](../P2_DIM_001_FEAT_维度与权重配置/01_功能需求规格说明书.md)（评分口径配置、活跃度阈值、配置生效时机）

### 7.4 变更记录

| 版本 | 日期 | 变更内容 | 变更人 |
|------|------|---------|--------|
| 1.0 | 2026-09-05 | 初始版本：evaluator/scorer/activity 三子域规格，C01-C13 逐条承接，四项设计决策（评分到落库为止、insufficient 标记+聚合剔除、末轮归属、0-100 整数分）经用户确认 | lixuetao |
| 1.1 | 2026-09-08 | 档案取数缓冲 24h → 7 天（实现修订回写：周周期内任意跨界形态全覆盖，缓冲窗内外推失真消除）；§2.4 能力4 判定次序澄清：列表量门槛对空列表同样生效（列表会话量为 0 → normal，独立调用退化形态不直判档案表型） | lixuetao |
---

> **文档生成信息**
> - 生成时间: 2026-09-05T03:12:00Z
> - 技能版本: 技术feature生成 v1.0
> - 技术组件ID: P2_TECH_005_TECH_综合评估与聚合

