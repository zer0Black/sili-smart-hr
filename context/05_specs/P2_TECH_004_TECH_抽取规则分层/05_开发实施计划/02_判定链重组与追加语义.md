# 判定链重组与追加语义 实现计划

> **目标：** 判定链入口变薄（classify.go 前缀匹配段委托 rules 组装器，锚点字面量随形态迁移，签名不变）；extractor.inject_prefixes 参数语义从替换改追加（读参 nil/[] 统一空追加集、装配 defaulter 移除该键、migrate seed 改空数组）；既有测试按追加语义更新预期并补分层结构测试（并集执行等价性、优先级保持、新客户端零侵入端到端、前缀去重）。
> **架构：** 子计划 01 已建立 rules 包并完成前缀迁移，本子计划把判定链的生效集来源切到组装器，并完成参数链路（seed → defaulter → 读参 → classifySequence 传入）的追加语义改造。行为等价性以基线全部表驱动用例全绿为验收线，替换语义两处预期翻转（05.2/5.4 已裁定）。
> **技术栈：** Go 1.25，GORM AutoMigrate seed（migrate.go），无新依赖。

---

## 文件结构

### 新建文件
- `hr-backend/internal/engine/extractor/rules_layering_test.go` — 并集执行等价性快照对照、判定优先级保持、新客户端零侵入端到端（Trim+DetectClient 面）、前缀去重判定一致性测试

### 修改文件
- `hr-backend/internal/worker/task/session_extract.go` — 任务契约超时配置落地（03 §6 勘误：handler ctx 预算以 ≥ 550s 为准），见 T6
- `hr-backend/internal/worker/task/health.go` — NewMux 内 session-extract 注册改挂 asynq.Timeout（T6：注册点在此文件的 mux.Handle，见 T6）
- `hr-backend/internal/engine/extractor/classify.go` — classifySequence 生效集计算改为调用 rules 组装器并集 + paramAppend（追加语义）；锚点字面量与通道归属注释按形态迁移；classifyMsg/classifyResult/classifiedMsg 签名不变
- `hr-backend/internal/engine/extractor/config.go` — narrowedPrefixes 语义调整（收窄集来源改 rules 并集，paramAppend 只做交集不替换）；ContinuationPrefix 收敛为引用 rules 常量
- `hr-backend/internal/engine/extractor/extract.go` — readParams/injectPrefixes 读参逻辑：inject_prefixes 的 nil（键缺失/损坏）与 []（显式清空）统一按空追加集处理；生效集 = 出厂并集 ∪ paramAppend
- `hr-backend/providers.go` — NewExtractorSysParamDefaults 移除 ParamKeyInjectPrefixes 键（回退 nil 即空追加）；NewExtractorSeed 注释与返回值同步收敛
- `hr-backend/internal/model/migrate.go` — inject_prefixes seed 值从 `extractor.InjectPrefixes()` 改为 `[]string{}`，description 改为追加语义文案
- `hr-backend/internal/model/migrate_test.go` — seed 断言更新：inject_prefixes 的 param_value 为 `[]`
- `hr-backend/internal/engine/extractor/trim_test.go` — TestClassifyCustomPrefixes / TestTrimCustomParams / TestTrimBlankPrefixEntry / TestInjectPrefixesFactoryAnchors 等涉替换语义的用例按追加语义更新预期
- `hr-backend/internal/engine/extractor/extract_test.go` — 参数联动相关 fake sysParams 用例按追加语义调整

---

## 数据模型来源

| 来源 | 文件路径 | 状态 |
|------|----------|------|
| 04_model_interface.md | `/context/05_specs/P2_TECH_004_TECH_抽取规则分层/04_model_interface.md` | 存在 |

引用：04_model_interface.md → system_params extractor.inject_prefixes seed 变更（§4，seed 值改空数组 `[]`，由 migrate.go 的 seedStringArrayParam 以 Go 代码构造，values 传 `[]string{}`）。这是 seed 数据变更（DML seed 行），随 migrate.go 代码任务落地，无独立迁移文件。session_features 表零变更（client 列归子计划 03）。

---

## 任务清单

- [x] **T1: 判定链生效集切到组装器（等价性保持）**

  **文件：**
  - 修改：`hr-backend/internal/engine/extractor/classify.go`
  - 修改：`hr-backend/internal/engine/extractor/config.go`
  - 测试：`hr-backend/internal/engine/extractor/rules_layering_test.go`（由实现者按 TDD 指导创建）

  **契约：**
  - 签名（不变项，锁死）：`classifyMsg(m conversationlog.Message, userPrefixes, nonUserPrefixes, redactPatterns []string) (res classifyResult)`；`classifySequence(msgs []conversationlog.Message, prefixes, redactPatterns []string) []classifiedMsg`；classifyResult/classifiedMsg 字段集零增减
  - 关键逻辑：
    1. classifySequence 会话级预计算改为：`userBase := rules 出厂并集（InjectPrefixes() 或直接调 rules 组装函数）`；当 `prefixes`（paramAppend）为 nil 或空时 effective = userBase，非空时 effective = `dedup(userBase ∪ prefixes)`。追加语义下 nil 与 [] 收敛同值（03 §5.3），不再存在「nil = 全量替换出厂」的旧语义
    2. 非 user 侧收窄：`narrowed = 非user收窄出厂并集 ∩ effective`（交集语义保持基线：paramAppend 通常不在收窄集，天然只作用 user 侧；删除配置条目不收回代码前缀）。narrowedPrefixes 函数签名保留，内部来源改 rules 并集
    3. classify.go 内的判定链锚点字面量（srTagOpen、cmdArgsOpen、interjectMarker、userQueryOpen、transcriptOpen 等）保持原位原值（判定优先级链是行为等价性的载体，specs §2.4 能力1 第 3 条：归属只影响代码组织与归属注释，不影响判定次序；本任务不搬函数体，只改前缀集来源与注释归属标注）。通道归属标注：1a 命令壳与 1b SR 分流、1c Base directory、插话通道注释补「归属 claude_code」；1b 内 user_query 提取分支注释补「归属 workbuddy」（specs §2.4 能力1 第 3 条）
    4. config.go：ContinuationPrefix 改为 `const ContinuationPrefix = rules.ContinuationPrefix` 引用单源（stats.go ContinuationHit 引用点零改动）
  - 依赖：`import "sili-smart-hr/backend/internal/engine/extractor/rules"`

  **验收锚点：**
  - 核心断言：`TestUnionExecutionEquivalence`——取基线表驱动用例的代表性消息样本（每形态至少一条：SR 分流、command 壳+args、command 纯壳、Base directory、Note: 打断预检、插话、打断标记、@CurrentContext、transcript 开闭区段、内嵌 SR 剥离、ide_selection、黑名单 OMO 转写 "TodoWrite "、碎片 "count"、收窄 assistant 侧 "<analysis>"、粘贴、口语体），分别以 `classifySequence(msgs, nil, nil)` 与 `classifySequence(msgs, []string{}, nil)` 断言两者逐消息 Class/Payload/InterruptCount 一致（追加语义 nil/[] 收敛），且与基线预期值一致
  - 验证命令：`cd hr-backend && go test ./internal/engine/extractor/ -run "TestUnionExecutionEquivalence|TestClassify|TestTrim" -v`
  - 预期输出：TestUnionExecutionEquivalence 与其余判定链用例 PASS；已知裁定差异三用例（TestClassifyCustomPrefixes、TestTrimCustomParams、TestTrimBlankPrefixEntry，替换语义预期在追加语义下翻转）本任务后转红属预期，预期更新在 T4 执行，不计入本任务验收门；除该三用例外零翻转

  **specs 依据：**
  - 章节范围：§2.4 能力1 第1条（并集执行不做定向路由）、第2条（生效集公式与收窄交集）、第3条（通道归属只影响组织不影响次序）、第4条（判定链入口变薄、classifyMsg 签名不变）、§5.1（并集执行等价性、判定优先级保持）
  - 业务规则索引：
    - [BR1] §2.4 能力1 第1条：通用规则与全部已注册客户端规则同时生效，与基线行为严格等价
    - [BR2] §2.4 能力1 第2条：非 user 角色按生效集与收窄子集的交集判定，基线语义不变
    - [BR3] §2.4 能力1 第4条：classifyMsg 函数签名与 classifyResult 返回结构不变，既有测试调用面稳定
    - [BR4] §5.1：判定次序与基线一致，提取类先于丢弃类、指纹先于噪音、内容通道先于黑名单
    - [BR5] 03 §5.3：loaded[inject_prefixes] 的 nil 与 [] 统一按空追加集处理，二者语义收敛

  **依赖：** 子计划 01 全部完成

  **提交信息：** refactor(extractor): 判定链生效集切 rules 并集组装器，nil/[] 追加语义收敛

- [x] **T2: 追加语义参数链路（seed 与 defaulter）**

  **文件：**
  - 修改：`hr-backend/internal/model/migrate.go:71`（seed 行）
  - 修改：`hr-backend/providers.go:196-208`
  - 测试：`hr-backend/internal/model/migrate_test.go`

  **契约：**
  - 签名（migrate.go seed 行改后形态）：
    ```go
    {extractor.ParamKeyInjectPrefixes, "会话裁剪注入前缀全局追加黑名单（JSON数组，只追加不替换，出厂集在代码中）", []string{}},
    ```
  - 签名（providers.go 改后形态）：
    ```go
    // NewExtractorSysParamDefaults 构造 defaulter map：redact_patterns 回退出厂，
    // inject_prefixes 回退 nil（空追加集，追加语义下无出厂回退概念）。
    func NewExtractorSysParamDefaults() map[string][]string {
        _, patterns := NewExtractorSeed()
        return map[string][]string{
            extractor.ParamKeyRedactPatterns: patterns,
        }
    }
    ```
  - 关键逻辑：
    1. seed 值 `[]string{}` 空集（04 §4 表格裁定），description 换追加语义文案（04 §4 SQL 注释同款）；redact_patterns 行零改动
    2. NewExtractorSeed 返回值语义调整：prefixes 返回值在 defaulter 不再消费，保留函数签名（该函数仅 providers.go 内部引用、无 wire_gen 生成产物引用，保留双返回值把改动收敛在 NewExtractorSysParamDefaults 单点；返回值可改为 nil 或保留 extractor.InjectPrefixes() 供 migrate 测试锚定，实现者选保留并注释用途）
    3. 存量库处置（04 §4）：不写存量行清洗钩子。开发库若有基线全量 seed 行残留，处置是手工删行或清库重建（按用户指令：数据库字段/数据变更按重新建表处理，开发环境直接删 `data/sili-smart-hr.db` 重建即可），不进迁移代码
  - 依赖：migrate_test 引用 `extractor.InjectPrefixes()` 的断言点同步改

  **验收锚点：**
  - 核心断言：`TestMigrateCreatesSessionFeatures` 更新——断言 inject_prefixes 行的 `ParamValue == "[]"`（空数组序列化）且 Version==1；redact_patterns 行保持出厂全集序列化不变；`TestSeedParamsIdempotent` 幂等断言保持通过
  - 验证命令：`cd hr-backend && go test ./internal/model/ -run "TestMigrateCreatesSessionFeatures|TestSeedParamsIdempotent" -v`
  - 预期输出：两个测试 PASS

  **specs 依据：**
  - 章节范围：04 §4（数据初始化变更表与 seed SQL 语义）、03 §5.3（参数键语义变更表：出厂 seed 空数组、键缺失回退空追加集、装配 defaulter 移除键）、§5.2（client 列建表与参数页追加场景的新预期）
  - 业务规则索引：
    - [BR1] 04 §4：inject_prefixes seed 值改空数组 []，配置集不承载出厂前缀
    - [BR2] 03 §5.3：defaulter map 移除 inject_prefixes 键，回退 nil 即空追加
    - [BR3] 04 §4：存量库不写清洗钩子，开发残留手工删行或清库重建（不进迁移代码）
    - [BR4] 03 §5.3 表：配置删除条目只收回运维追加条目，代码前缀并集不受配置影响

  **依赖：** 无（与 T1 并行安全，migrate/providers 与 classify 不同文件；提交顺序 T1 先）

  **提交信息：** feat(model): inject_prefixes seed 改空集追加语义，defaulter 移除该键

- [x] **T3: 读参消费侧追加组装**

  **文件：**
  - 修改：`hr-backend/internal/engine/extractor/extract.go:73-94`
  - 测试：`hr-backend/internal/engine/extractor/extract_test.go`

  **契约：**
  - 签名（extract.go 改后逻辑形态）：
    ```go
    // injectPrefixes 计算运行时生效黑名单：出厂并集 ∪ paramAppend（追加语义）。
    // prefixes 为 nil（键缺失/损坏，defaulter 已移除该键）或 []（显式清空）均按空追加集。
    func (e *Extractor) injectPrefixes(prefixes []string) []string {
        return blankEntriesRemoved(append(InjectPrefixes(), blankEntriesRemoved(prefixes)...))
        // 去重由 classifySequence 侧会话级预计算收敛（或在此处 dedup，实现者按单一职责选址，
        // 等价性断言锚定最终 effective 集排序后与 出厂并集∪追加集 去重相等）
    }
    ```
  - 关键逻辑：原 nil 分支「回退 InjectPrefixes()」语义在追加模式下变成恒等式（nil → 空追加 → effective = 出厂并集），函数保留但分支收敛；redactPatterns 零改动（替换语义维持）；readParams 零改动
  - 行为差异锚定（03 §5.4，两处预期翻转是裁定非回归）：配置集删除出厂前缀不再收回该前缀；配置显式 [] 不再放开全部过滤（生效集仍含出厂并集）

  **验收锚点：**
  - 核心断言：`TestInjectPrefixesAppendSemantics`——fake sysParams 返回 `{ParamKeyInjectPrefixes: []string{"ZZZ-custom-prefix"}}` 时，含 "ZZZ-custom-prefix" 打头 user 消息被 drop 且含出厂前缀（如 "Note:"）消息仍被 drop（追加不替换）；返回 `[]string{}` 时出厂前缀仍全量生效（显式清空 = 无追加）；返回 nil（键缺失）时同 []；返回 `[]string{""}` 空串条目时被过滤不误杀
  - 验证命令：`cd hr-backend && go test ./internal/engine/extractor/ -run TestInjectPrefixesAppendSemantics -v`
  - 预期输出：`--- PASS: TestInjectPrefixesAppendSemantics`（TestClassifyCustomPrefixes / TestTrimCustomParams 两用例在 T1 后转红、T4 更新预期后才恢复 PASS，其验收归 T4 验收门，此处不跑以免门内必败）

  **specs 依据：**
  - 章节范围：03 §5.3（消费侧变更：nil 与 [] 统一空追加集、生效集按 §5.1 公式组装、redact 零改动）、§5.4（与基线集成测试的已知行为差异：替换语义预期翻转是裁定）、§5.2（参数页全局追加场景）
  - 业务规则索引：
    - [BR1] 03 §5.3：nil（键缺失/损坏）与 []（显式清空）统一按空追加集处理，无需区分
    - [BR2] 03 §5.1：effective = 通用层前缀 ∪ 客户端并集 ∪ paramAppend，追加集对全部客户端消息生效
    - [BR3] 03 §5.4：配置删除出厂前缀不再收回该前缀，属裁定的行为变更而非回归
    - [BR4] 03 §5.3 表：显式清空 [] 语义 = 无追加，生效集 = 出厂并集（非放开全部过滤）

  **依赖：** T1

  **提交信息：** feat(extractor): 读参消费侧改追加组装，nil/[]/空串条目口径收敛

- [x] **T4: 既有测试按追加语义更新 + 优先级保持测试**

  **文件：**
  - 修改：`hr-backend/internal/engine/extractor/trim_test.go`
  - 测试：`hr-backend/internal/engine/extractor/rules_layering_test.go`

  **契约：**
  - 签名（新增测试）：
    ```go
    // TestPriorityChainPreserved 构造优先级冲突样本（command 壳 + 内嵌打断 + 黑名单前缀
    // 叠加的消息），断言判定次序与基线一致（specs §5.1 判定优先级保持场景）。
    func TestPriorityChainPreserved(t *testing.T)
    ```
  - 关键逻辑：
    1. 既有用例预期更新清单（替换→追加语义翻转点）：TestClassifyCustomPrefixes（传 `[]string{"Note:"}` 时基线预期是只拦 Note: 放开 User: —— 追加语义下 User: 仍被出厂并集拦截，预期翻转）；TestTrimCustomParams 同理；TestTrimBlankPrefixEntry（空前缀条目过滤后空集语义从「全量放开」变「无追加」）；TestInjectPrefixesFactoryAnchors（锚定集来源改组装器，断言不变或迁移至子计划 01 的快照测试后删除重复）；extract_test.go 中 fake sysParams 注入参数的用例逐个核对预期
    2. TestPriorityChainPreserved 样本设计：同一条 user 消息同时命中 command 壳打头（1a 优先）、正文含打断标记（计数透出）、args 内文以黑名单前缀打头（提取通道优先于黑名单）；断言走 classExtract + InterruptCount == 1 + Payload 为剥标后 args；再构造黑名单打头的 assistant 消息断言收窄交集（"<analysis>" 打头 drop、"TodoWrite " 打头保留进 [AI]）
    3. 更新原则：只改预期值与断言语义，测试结构、消息构造器（mkMsg/mkRoleMsg/mkToolMsg）零改动
  - 依赖：T1, T3

  **验收锚点：**
  - 核心断言：`TestPriorityChainPreserved`——断言 conflict 样本的 classifyResult 为 `{Class: classExtract, InterruptCount: 1}` 且 Payload 等于剥标后 args 字面；assistant 收窄样本断言 "<analysis>" 消息 classDrop、"TodoWrite " 消息 classKeep
  - 验证命令：`cd hr-backend && go test ./internal/engine/extractor/... -v`
  - 预期输出：extractor 与 rules 两包全部测试 PASS（含更新后的既有用例）

  **specs 依据：**
  - 章节范围：§5.1（判定优先级保持场景、基线全部表驱动用例作为等价性验收线）、03 §5.4（基线集成测试预期翻转清单）、§5.2（黑名单参数联动按追加语义更新预期）
  - 业务规则索引：
    - [BR1] §5.1：构造优先级冲突样本，判定次序与基线一致（提取先于丢弃、指纹先于噪音、内容通道先于黑名单）
    - [BR2] 03 §5.4：基线替换语义预期按追加语义翻转，属裁定的行为变更
    - [BR3] §5.1 首段：基线全部测试场景在新分层结构下全部保留并全绿，作为行为等价性验收线

  **依赖：** T1, T3

  **提交信息：** test(extractor): 既有用例按追加语义更新预期，补优先级保持测试

- [x] **T5: 新客户端零侵入端到端与前缀去重测试**

  **文件：**
  - 测试：`hr-backend/internal/engine/extractor/rules_layering_test.go`

  **契约：**
  - 签名（新增测试，均落在 extractor 内部测试包以驱动 Trim 与 rules 面）：
    ```go
    // TestNewClientZeroIntrusion 测试内注册 fake ClientRules（专属前缀 "<newclient-frame>"），
    // 对含该前缀的会话跑 Trim 断言新前缀按并集生效（drop）。
    func TestNewClientZeroIntrusion(t *testing.T)

    // TestPrefixDedupJudgement 构造 generic 与 fake 客户端贡献相同前缀字面（"Note:"），
    // 断言并集去重后判定结果与单客户端贡献一致（一次 HasPrefix 命中即 drop，无重复开销面）。
    func TestPrefixDedupJudgement(t *testing.T)
    ```
  - 关键逻辑：fake 规则用测试内局部类型实现 ClientRules 四方法并 Register（注册表只增不减，测试内注册污染全局注册表的影响：TestNewClientZeroIntrusion 注册的前缀会进后续测试的 effective 集——用形态独特的前缀字面（"<newclient-frame>"）规避对其他用例的干扰，文件内注释说明该约束）；Trim 用 Extractor 实例 + fake sysParams（nil 追加集）驱动。DetectClient 面的零侵入断言归子计划 03 T3（TestDetectClientNewClientZeroIntrusion 承接），本任务只验判定链面
  - 依赖：T1

  **验收锚点：**
  - 核心断言：`TestNewClientZeroIntrusion`——含 "<newclient-frame>" 打头 user 消息的会话 Trim 后该消息被丢弃（TrimStats.InjectedDropped 含此条），既有客户端前缀（"Note:"）行为不变；`TestPrefixDedupJudgement`——effective 集中 "Note:" 仅出现一次，"Note:" 打头消息判定为 drop 与基线一致
  - 验证命令：`cd hr-backend && go test ./internal/engine/extractor/ -run "TestNewClientZeroIntrusion|TestPrefixDedupJudgement" -v`
  - 预期输出：两个测试 PASS

  **specs 依据：**
  - 章节范围：§5.1（新客户端接入零侵入、前缀去重两场景）、§2.5 高级用法（新客户端接入示例）、§2.4 能力8（接入步骤与既有文件零改动承诺）
  - 业务规则索引：
    - [BR1] §5.1：测试内注册 fake ClientRules，新前缀按并集生效（drop），既有客户端规则行为无变化
    - [BR2] §5.1：构造两个客户端贡献相同前缀字面，并集去重后判定结果与单客户端贡献一致
    - [BR3] §2.4 能力8：新客户端接入 = 新增规则文件 + 一行注册，既有客户端文件零改动

  **依赖：** T1

  **提交信息：** test(extractor): 新客户端零侵入端到端与前缀去重判定测试

- [x] **T6: Asynq 任务超时配置 550s 落地（03 §6 勘误闭环）**

  **文件：**
  - 修改：`hr-backend/internal/worker/task/session_extract.go`
  - 修改：`hr-backend/internal/worker/task/health.go`（NewMux 是任务注册点，现以 `mux.Handle(TypeSessionExtract, sessionExtract)` 注册 bare handler，挂任务级超时须在此换 `mux.HandleFuncWithOptions(TypeSessionExtract, handler, asynq.Timeout(sessionExtractTimeout))` 或等价注册形态，按 asynq 版本 API 选址；NewMux 入参是 asynq.HandlerFunc，改注册形态时入参签名可保持不变——handler 值直接经 HandleFuncWithOptions 注册）
  - 测试：`hr-backend/internal/worker/task/`（若该包无既有测试文件则补最小断言：Mux 注册的任务 options 携带 Timeout ≥ 550s）

  **契约：**
  - 签名（handler 注册形态改后）：
    ```go
    // engine:session-extract 任务超时（03 §6 勘误：LLM 重试双调用 360s +
    // 详情拉取最坏 190s 全链约 550s，Retry-After 封顶 60s 已计入，基线 specs §2.3 口径）。
    const sessionExtractTimeout = 550 * time.Second

    // NewMux 注册时经 asynq.Timeout(sessionExtractTimeout) 挂任务级超时，
    // 与 providers.go:139 注释口径（≥ 550s）对齐，替代基线计划「310s 预算由 ctx 链承载」的滞后口径。
    ```
  - 关键逻辑：mux.HandleFuncWithOptions(TypeSessionExtract, handler, asynq.Timeout(sessionExtractTimeout))（或等价注册形态，按 asynq 版本 API 选址）；基线 03 §3.3 的 440s 与基线计划 03 的 310s 均属 specs v1.1 修订前滞后口径，以本任务为唯一落地点；payload schema 与错误分流零改动
  - 依赖：无（与 T1-T5 无共享文件，串行执行顺序内任意时点可做；放本子计划因 03 §6 勘误随本 Feature 文档链披露）

  **验收锚点：**
  - 核心断言：注册的任务 options 中 Timeout == 550s（反射或 asynq 内省断言；若包结构不便断言则以启动冒烟 + 代码审查锚定并在测试注释说明）
  - 验证命令：`cd hr-backend && go test ./internal/worker/... -v`
  - 预期输出：全部 PASS

  **specs 依据：**
  - 章节范围：03 §6（勘误：handler ctx 预算以基线 specs §2.3 的 ≥ 550s 为准，dev_plan 排超时配置任务时以 550s 落地）
  - 业务规则索引：
    - [BR1] 03 §6：任务 ctx 预算 ≥ 550s（LLM 重试双调用 360s + 详情拉取最坏 190s，Retry-After 封顶 60s 已计入）
    - [BR2] 基线 specs §2.3：超时推导唯一权威口径，基线 03 §3.3 的 440s 属修订前滞后

  **依赖：** 无

  **提交信息：** feat(worker): session-extract 任务超时 550s 落地（03 §6 勘误闭环）
